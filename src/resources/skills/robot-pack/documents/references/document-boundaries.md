# Document Boundaries

Markdown belongs under docs/ by repository convention. DOCX rendering and Google Docs import require the configured documents runtime; do not substitute unreviewed container scripts or plaintext secrets.