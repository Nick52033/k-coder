# 现有插件适配与启用验证

日期：2026-09-14。任务：P10-181。

## 当前范围与实现

以当前项目 `.k-coder/plugins` 的七个目录为准；没有恢复用户已移除的插件。此前导入记录描述的是历史快照，本次实际数量为七个。

| 插件 | 本地适配 | 功能边界 |
| --- | --- | --- |
| browser | Skill 使用 browser_navigate/snapshot/click/type/screenshot/close | 受控浏览器与右侧预览 iframe 是不同会话；无 Codex node_repl/iab 接口 |
| documents | 明确的工作区 PowerShell 启动器调用已安装 Python，保留 DOCX 编写/OOXML 指引 | 缺少 LibreOffice，页面渲染未通过；无 Google Docs 云端导入 |
| presentations | 小写 Skill 名称，artifact-tool 公共 ESM 导入，取消依赖目录联接 | PPTX 可导出和复读；现有原生渲染器有退出异常，无 Google Slides 云端导入 |
| spreadsheets | 小写 Skill 名称，artifact-tool 构建与公式/缓存复读 | XLSX 可导出；现有原生渲染器有退出异常，无实时 Excel/Google Sheets 连接器 |
| record-replay | 本地 Playwright 结构录制/回放入口，修复生成 Skill 元数据，映射已录制流程 | 输入值和导航 URL 均参数化；登录示例要求手动认证，不声称专用回放面板或自动修复 UI 已存在 |
| sites | 本地建站/构建/预览流程复用现有工具与模板 | OpenAI Sites Apps 连接器未接入，仍应显示部分可用；没有发布网站 |
| superpowers | 14 个 Skill 的读取、计划、文件、Shell 和委派工具映射 | 遵守实际工具 Schema 和项目指令，不修改 Codex 配置，不默认提交 |

宿主不再把 `interface` 展示元数据计入“不支持组件”；仍校验其资源路径。Apps、Hooks、agents、commands 的诊断和所有路径边界保持原有语义。新增回归先在旧逻辑稳定失败（Degraded != Loaded），修改后通过。

## 已执行验证

- `pnpm build` 通过，仍有既有大 chunk 提示。
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过。
- `cargo check --manifest-path src-tauri/Cargo.toml` 通过。
- 插件 Rust 专项 33/33 通过。
- 全量 `cargo test --manifest-path src-tauri/Cargo.toml -- --test-threads=1`：546 通过、3 个此前记录的读取收敛失败。失败名称为 `read_recovery_delivery_only_hard_stops_the_corrected_provider_batch`、`recovery_still_stops_varied_overlapping_reads_after_one_correction`、`semantic_read_tracker_recovers_once_before_stopping_overlap_loops`；与 P10-175/P10-179/P10-180 记录相同。
- 插件设置 Playwright 专项：6 通过，2 个窄屏用例按原配置跳过。
- 文档启动器、ESM 导入和路径边界测试 7/7 通过，执行方式使用 `plugin-artifact-runtime.ps1`。
- 录制回放测试 9/9 通过：真实 Edge 结构录制和六类浏览器动作回放、录制超时、输入值及 URL 不落盘、敏感/截断工作流拒绝、导入流程参数解析、非法工具拒绝、路径/目录联接/NTFS 流边界、脚本超时和取消。`doctor` 正确定位系统 Edge 与已安装 Playwright；避免依赖当前版本不导出的 `playwright/cli`。
- DOCX/XLSX/PPTX 实际生成与独立 Python 重开通过，XLSX 公式 `=B2*2` 缓存结果为 42；PPTX 可编辑文字保留，三种 OOXML ZIP 完整性通过。详情见 `插件文档运行时验证.md`。
- artifact-tool 预览 PNG 实际写出并可查看，但 Node 退出为 `-1073740791`；使用系统 Node 24.18.1 同样复现，没有隐藏错误码。

## 原生验收与正式启用

已启动隔离 `pnpm tauri dev --no-watch --config .tmp-ui/plugin-adaptation/tauri.conf.json`，使用 Vite 1482 / WebView2 CDP 9422 / `com.kcoder.validation.plugins`。真实 WebView 经本地测试 Provider 驱动同一个 AgentRuntime，完成七次 `plugin_skill_read`、计划更新、文档运行时探测以及导航、快照、输入、点击、截图、关闭六类浏览器动作；断言页面结果与 PNG 有效。刷新后七个启用开关保持打开。结果见 `插件适配原生结果.json`，界面见 `插件适配原生验证.png`，可复执行脚本为 `scripts/validate-plugins-native.cjs`。

正式数据目录为 `%APPDATA%/com.kcoder.app/runtime-data`。使用本次源码构建的临时 Rust 入口调用已有 `ExtensionService::set_plugin_enabled`，逐个启用七个索引 ID 并经 `plugin_overview` 回读，不直接更新数据库，不绕过插件启停审计。临时入口源码已移除，实际返回保存为 `插件正式启用结果.json`。七个 `enabled` 均为 true，六个 `loaded`，Sites 因 `.app.json` 仍为 `degraded`；没有 `invalid`。正式启用仅更改插件开关，没有更改正式模型配置或审批设置。

“已加载”代表本地 Skill 可被宿主加载，不等于所有外部运行时或原客户端云能力已完成接入。文档渲染和云连接器的限制见上表及专项文档，不能用取消诊断的方式掩盖。

首次开发宿主构建遇到目标 EXE 临时占用，进程调查确认只有既有安装进程；再次执行构建启动成功，没有终止既有客户端。

源码与测试改动仅位于 D:/code/k-coder。没有部署到 D:/apps/k-coder，也没有修改当前正在运行的旧 E:/Program Files/k-coder 安装目录。没有提交或推送。

验收完成后已按进程路径和启动命令确认身份，停止本轮隔离开发宿主及其子进程；旧安装版进程保持运行。旧安装版需要刷新插件以重建当前工具注册表；`interface` 状态判定的源码修正需要更新客户端后才会显示。
