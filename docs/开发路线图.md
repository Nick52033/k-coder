# k-Coder 开发路线图

本文档是项目开发顺序的唯一事实来源。它应持续维护，直到 k-Coder 的核心能力接近 cn-codex，并达到成熟编程智能体的水平。

## 当前位置

| 项目 | 当前值 |
| --- | --- |
| 最后更新 | 2026-08-17 |
| 当前阶段 | Phase 10 - 生产加固 |
| 当前状态 | 用户优先插入的 `P10-123` 对话输入区快捷操作与上下文选择器层级调整已完成 |
| 最新完成 | `P10-123` 对话输入区快捷操作与上下文选择器层级调整 |
| 下一项任务 | `P10-078` 精简 Monaco ESM 与 worker 加载 |
| 目标版本 | `1.0.0` |

完成任务时，必须在同一次修改中更新本表和对应的任务复选框。

## 进度标记

- `[ ]` 尚未开始
- `[~]` 正在进行
- `[x]` 已完成并验证
- `[!]` 已阻塞，必须在任务旁写明原因
- 可选任务会明确标记，不会阻塞后续核心阶段

## 推进原则

各阶段必须按顺序完成。可以提前调研后续阶段，但在当前阶段通过验收门槛之前，不应合入后续阶段的生产代码。这样可以避免用大量界面和集成功能掩盖不可靠的核心运行时。

---

## Phase 0 - 项目基础

目标：建立可构建的桌面项目、稳定的模块职责、类型化 IPC 和开发规则。

- [x] `P0-001` 使用 pnpm 创建 Tauri 2 + React + TypeScript 项目。
- [x] `P0-002` 建立 agent、providers、tools、policy、storage、protocol 和 commands Rust 模块。
- [x] `P0-003` 添加类型化 `runtime_status` Tauri 命令。
- [x] `P0-004` 用 k-Coder 工作台框架替换脚手架演示页面。
- [x] `P0-005` 添加架构、ADR、路线图和仓库智能体说明。
- [x] `P0-006` 安装依赖并通过前端、Rust、测试和桌面启动检查。
- [ ] `P0-007` 可选：由项目所有者提出要求后初始化 Git 并创建基础提交。

验收门槛：

- `pnpm build` 通过。
- `cargo fmt --check`、`cargo check` 和 `cargo test` 通过。
- `pnpm tauri dev` 能打开桌面窗口，界面显示"运行时就绪"。
- Tauri 命令处理器和 React 组件中不存在智能体业务逻辑。

交付版本：`0.1.0-foundation`。

---

## Phase 1 - 流式对话运行时

目标：通过与供应商无关、可持久化、可取消的 `AgentRuntime` 完成真实的纯文本对话。

- [x] `P1-001` 添加 Rust 单元测试结构、测试夹具和确定性的 `FakeProvider`。
- [x] `P1-002` 定义版本化内部消息、内容、用量、Provider 事件和错误类型。
- [x] `P1-003` 把 Provider 契约改为支持取消的真实异步事件流。
- [x] `P1-004` 实现模型和 Provider 配置校验。
- [x] `P1-005` 使用操作系统凭据设施保存 API Key。
- [x] `P1-006` 实现 OpenAI-compatible Chat Completions SSE 适配器。
- [x] `P1-007` 实现纯文本 `AgentRuntime.run_turn` 循环。
- [x] `P1-008` 添加开始、独立活动状态、安全推理摘要、文本增量、用量、完成、失败和取消事件。
- [x] `P1-009` 实现支持崩溃安全追加和恢复的 JSONL 会话事件。
- [x] `P1-010` 添加创建、列表、读取和归档会话命令。
- [x] `P1-011` 接通界面会话状态、流式消息、重试和停止控制。
- [x] `P1-012` 为分片、格式错误、错误响应和中断流添加适配器解析夹具。

验收门槛：

- 用户可以配置一个 Provider 并进行多轮纯文本对话。
- 关闭并重新打开应用后，会话和消息可以恢复。
- 点击停止能够及时取消 HTTP 流，并持久化 `turn_cancelled`。
- Provider 测试不依赖真实网络。
- API Key 不得出现在界面载荷、JSONL、SQLite 或日志中。

交付版本：`0.2.0-chat`。

---

## Phase 2 - 原生工具调用循环

目标：允许模型通过原生 Function Calling 检查工作区，但暂不提供写权限。

- [x] `P2-001` 实现包含稳定工具名和 JSON Schema 的 `ToolRegistry`。
- [x] `P2-002` 在工具分发前完成 Schema 校验。
- [x] `P2-003` 实现工作区根目录规范化解析。
- [x] `P2-004` 拒绝绝对路径、父目录穿越、符号链接逃逸和目录联接逃逸。
- [x] `P2-005` 实现带数量限制和忽略目录规则的 `list_directory`。
- [x] `P2-006` 实现带大小限制和编码识别的 `read_file`。
- [x] `P2-007` 实现结构化工具调用和工具结果历史消息。
- [x] `P2-008` 工具返回结果后继续同一个 Turn，直到得到最终文本或达到循环上限。
- [x] `P2-009` 添加工具生命周期、实时输出事件和界面工具调用记录。
- [x] `P2-010` 添加最大调用数、最大迭代数、输出限制和重复调用保护。
- [x] `P2-011` 测试无效参数、未知工具、边界路径、取消和不完整工具调用。

验收门槛：

- 模型可以只使用已注册的只读工具回答仓库问题。
- 所有只读工具都无法逃逸已选择工作区。
- 模型请求的每次工具调用都有且只有一个持久化结构化结果。
- 无限或重复工具循环会以明确的类型化原因结束。

交付版本：`0.3.0-read-agent`。

---

## Phase 3 - 安全编辑与审阅

目标：通过可检查、可审批的 Diff 流程完成确定性的代码修改。

- [x] `P3-001` 把严格 Patch 解析实现为独立领域模块。
- [x] `P3-002` 实现支持新增、修改、删除和移动的 `apply_patch`。
- [x] `P3-003` 添加修改前哈希，避免覆盖规划后又被改变的文件。
- [x] `P3-004` 只在明确需要完整文件替换时使用 `write_file`。
- [x] `P3-005` 在 `PolicyEngine` 中定义读取、写入、删除和外部访问风险等级。
- [x] `P3-006` 实现审批请求、通过、拒绝和超时生命周期。
- [x] `P3-007` 添加待审阅 Patch，支持按文件选择和编辑 Patch 内容。
- [x] `P3-008` 添加统一 Diff 和并排 Diff 界面。
- [x] `P3-009` 在大小限制内记录文件变更摘要及修改前后快照。
- [x] `P3-010` 为部分失败的多文件 Patch 添加回滚行为。

验收门槛：

- 不允许通过自由文本中的代码块直接修改文件。
- 审批被拒绝或超时后，文件系统不得发生改变。
- Patch 冲突必须明确报告，不能静默覆盖较新的内容。
- Diff 必须准确展示全部已应用变更。
- 写入、删除、重命名和符号链接场景都要覆盖工作区逃逸测试。

交付版本：`0.4.0-edit-agent`。

---

## Phase 4 - 命令与终端运行时

目标：通过受控、可取消的进程运行时执行构建和测试。

- [x] `P4-001` 定义前台和后台命令会话契约。
- [x] `P4-002` 实现平台感知的命令启动，禁止使用跨 Shell 拼接字符串执行删除操作。
- [x] `P4-003` 实现 stdout/stderr 流、游标读取、有界缓冲区和对话内增量投影。
- [x] `P4-004` 在 Windows、macOS 和 Linux 实现超时及进程树终止。
- [x] `P4-005` 实现命令状态、等待、标准输入和关闭操作。
- [x] `P4-006` 添加命令风险分类，审批原因由运行时生成。
- [x] `P4-007` 添加可配置的可复用授权，但不允许模型定义授权范围。
- [x] `P4-008` 把基于 PTY 的交互终端作为独立界面和运行时能力。
- [x] `P4-009` 从日志、最终工具结果和实时输出事件中移除密钥及敏感环境变量。
- [x] `P4-010` 为成功、非零退出、超时、取消和后代进程清理添加集成测试。

验收门槛：

- 智能体可以运行项目测试，并在同一个 Turn 中读取测试结果。
- 取消操作能够终止完整的子进程树。
- 命令不能通过修改工具参数绕过审批。
- 长时间运行的会话可以恢复查询并被显式关闭。

交付版本：`0.5.0-execution-agent`。

---

## Phase 5 - 上下文、持久化与恢复

目标：在长对话、大型仓库、崩溃和升级场景下保持可靠。

- [x] `P5-001` 添加 SQLite Migration，以及会话列表、设置和用量投影。
- [x] `P5-002` 保持 JSONL 为对话事件的持久化事实来源。
- [x] `P5-003` 支持从 JSONL 重新构建 SQLite 会话投影。
- [x] `P5-004` 统计每次 Provider 调用和每个 Turn 的 Token 用量。
- [x] `P5-005` 为 System、规则、历史、工具和用户输入实现上下文预算分配器。
- [x] `P5-006` 使用确定性摘要契约实现手动 Compaction。
- [x] `P5-007` 实现 Turn 前和 Turn 中自动 Compaction 阈值。
- [x] `P5-008` Compaction 后保留关键用户约束和近期工具结果。
- [x] `P5-009` 异常退出后恢复未完成的 Turn 和命令会话。
- [x] `P5-010` 添加 Schema/事件版本及迁移测试。
- [x] `P5-011` 添加带脱敏和本地轮转的结构化日志。

验收门槛：

- 长会话能够保持在配置的上下文限制内。
- Compaction 不得删除当前任务或尚未处理的审批。
- JSONL 最后一行损坏或被截断时，可以保留并恢复之前的事件。
- 每个已发布数据库版本都必须有前向迁移测试。

交付版本：`0.6.0-resilient-agent`。

---

## Phase 6 - 编程工作台

目标：提供桌面编程智能体日常使用所需的项目工作流。

- [x] `P6-001` 添加项目注册表、最近工作区和工作区切换。
- [x] `P6-002` 添加支持忽略路径和刷新事件的懒加载文件树。
- [x] `P6-003` 添加文件预览、在编辑器中打开和在资源管理器中定位。
- [x] `P6-004` 添加 Git 状态、Diff、暂存、取消暂存、提交、分支、拉取和推送服务。
- [x] `P6-005` 为破坏性 Git 操作设置独立审批。
- [x] `P6-006` 添加图片附件和有界文档内容提取。
- [x] `P6-007` 添加模型/Provider 设置、连接测试和用量界面。
- [x] `P6-008` 添加可搜索的会话历史、重命名、归档和删除。
- [x] `P6-009` 添加由持久化运行时事件驱动的计划展示。
- [x] `P6-010` 添加无障碍访问、键盘导航、响应式布局和错误恢复。
- [x] `P6-011` 为主要"编辑并测试"工作流添加桌面端到端测试。

验收门槛：

- 用户可以选择仓库、提出修改、审阅变更、运行测试并检查 Git 状态，全程无需离开 k-Coder。
- 页面重载后，界面状态可以从运行时状态重新构建。
- 前端 Store 不能成为持久化智能体状态的事实来源。

交付版本：`0.7.0-workbench`。

---

## Phase 7 - Skills、规则、Hooks 与 MCP

目标：在不削弱核心运行时安全性的前提下支持可控扩展。

- [x] `P7-001` 加载全局和项目级指令文件，并明确优先级。
- [x] `P7-002` 定义并校验 `SKILL.md` 元数据和发现规则。
- [x] `P7-003` 要求智能体执行前读取所选 Skill 指令。
- [x] `P7-004` 添加 Skill 风险元数据和运行时策略判断。
- [x] `P7-005` 实现 MCP stdio 生命周期、初始化、工具列表和工具调用。
- [x] `P7-006` stdio 稳定后再实现 MCP HTTP 传输。
- [x] `P7-007` 对 MCP 工具使用命名空间，并确定性处理名称冲突。
- [x] `P7-008` 注入 MCP 密钥，但不得把密钥暴露给模型上下文或日志。
- [x] `P7-009` 添加带超时、类型化输出和阻止决策的工具前后 Hooks。
- [x] `P7-010` 添加扩展诊断、启用/停用控制和审计历史。

验收门槛：

- 格式错误的 Skill、Hook 和 MCP 配置必须以清晰错误关闭，不能降级放行。
- 外部工具必须经过与内置工具相同的策略、审批、超时和审计流程。
- 扩展不能静默替换内置工具。

交付版本：`0.8.0-extensible-agent`。

---

## Phase 8 - 多智能体运行时

目标：在保持权限、取消和预算约束的同时安全委派独立工作。

- [x] `P8-001` 子智能体复用 `AgentRuntime`，不得实现第二套循环。
- [x] `P8-002` 定义创建、等待、发送消息、恢复、列表和关闭契约。
- [x] `P8-003` 限制最大深度、活动数量和运行时长，并支持宿主显式设置的可选 Token 预算。
- [x] `P8-004` 为每个子智能体设置明确的工作区和工具 Capability。
- [x] `P8-005` 实现父子智能体取消传播。
- [x] `P8-006` 通过租约或冲突检测阻止多个智能体并发写入同一路径。
- [x] `P8-007` 持久化子智能体生命周期和摘要输出。
- [x] `P8-008` 添加运行中、阻塞、失败和完成状态的活动界面。
- [x] `P8-009` 为并行读取、写冲突、取消和超时添加确定性测试。
- [x] `P8-010` 在默认启用委派前，通过评估证明它确实改善了选定任务。

验收门槛：

- 关闭子智能体必须真正取消它的模型请求和活动工具。
- 子智能体不能超出父智能体的权限和预算。
- 并行工作不能静默覆盖其他智能体的变更。
- 主智能体接收结构化结果，而不是原始隐藏对话历史。

交付版本：`0.9.0-multi-agent`。

---

## Phase 9 - 高级智能体能力

目标：只在核心运行时经过测量且足够可靠后，增加高价值自治能力。

- [x] `P9-001` 添加显式计划生命周期，只允许一个步骤处于活动状态，并持久化更新。
- [x] `P9-002` 添加带时间预算和可选 Token 预算的 Goal 模式，支持暂停、恢复、阻塞、完成和预算耗尽状态。
- [x] `P9-003` 在独立权限边界后添加浏览器自动化。
- [x] `P9-004` 添加截图/产物处理和浏览器取消。
- [x] `P9-005` 添加仓库搜索和索引，优先实现确定性的词法搜索。
- [x] `P9-006` 添加可选择启用的记忆，并提供来源、编辑、删除和保留期限控制。
- [x] `P9-007` 先定义失败语义，再添加模型回退和多端点能力。
- [x] `P9-008` 添加带文件大小、类型和提取上限的文档解析。
- [x] `P9-009` 使用真实编程任务和录制的 Provider 夹具构建回归评估。
- [x] `P9-010` 添加成本、延迟、工具成功率、重试和任务完成指标。

评估后可选：

- [x] 机器人或工作流定义：完成三个受限内置机器人；自定义定义与可视化编排继续延后。
- [ ] 浏览器录制和回放。
- [ ] 手机远程控制和中继服务器。
- [ ] 插件市场和远程安装。

验收门槛：

- 自治模式始终有可见的累计消耗、资源边界和明确终止状态。
- 记忆和浏览器访问必须由用户选择启用，并且可以审计。
- 评估证明安全性和普通对话/编辑工作流没有明显回退。

交付版本：`0.10.0-advanced-agent`。

---

## Phase 10 - 生产加固

目标：让版本具备可支持、可升级、安全和可诊断能力。

- [ ] `P10-001` 对模型输入、工具、工作区边界、MCP、Hooks 和更新机制进行威胁建模。
- [ ] `P10-002` 在平台支持时添加操作系统级命令沙箱，并记录平台差异。
- [ ] `P10-003` 添加签名安装包、更新元数据、版本回滚和发布通道。
- [ ] `P10-004` 添加用户数据备份、导出和导入。
- [ ] `P10-005` 添加默认保存在本地且会移除用户内容的崩溃报告。
- [ ] `P10-006` 为启动时间、内存、流式延迟和大型会话设定性能预算。
- [ ] `P10-007` 在 CI 中测试 Windows、macOS 和 Linux 打包。
- [ ] `P10-008` 添加依赖、许可证、密钥和漏洞扫描。
- [ ] `P10-009` 对受支持 Provider API 夹具进行兼容性测试。
- [ ] `P10-010` 发布数据位置、恢复方法和安全限制等运维文档。
- [x] `P10-111` 用户反馈模型在同一批读取中猜测不存在的文件路径：`read_file` 错误诊断在父目录不存在时增加有界、深度受限且拒绝链接逃逸的工作区候选扫描，可提示跨目录/文件名变化的精确候选但不自动模糊读取；工作区 system prompt 明确要求先单独发现并等待 `list_directory`/`search_repository` 结果，禁止同一批并行发起猜测的文件读取，并新增 Rust 回归测试。
- [x] `P10-112` 用户明确调整优先级：新增全局 `runtime-data/mcp.json` 与项目 `.k-coder/mcp.json` 专用配置，按全局旧配置、全局专用配置、项目旧配置、项目专用配置合并并保留 `extensions.json` 兼容；设置中心提供双作用域 JSON 直接编辑、格式化、即时校验、stdio/Streamable HTTP 运行诊断、系统凭据维护和响应式布局，不维护重复的可视化表单状态；后端严格校验并拒绝链接逃逸，密钥继续只进入操作系统凭据存储。
- [x] `P10-113` 用户反馈长 Turn 在上下文压缩后突然停止：Provider 历史把结构化 `CompactionSummary` 与真实消息分离，只在请求边界渲染摘要；当前请求、近期请求和约束仅从持久化 `UserMessage` 重建，重复压缩不再把 `[Compacted context vN]` 当成新用户请求。契约升级为 v4，并从原始 JSONL 真实用户事件恢复已被递归 v3 摘要污染的旧会话，不改写事实日志。
- [x] `P10-114` 用户明确把普通、无活动 Goal 的根 Turn 分段软续跑请求门槛从 `30` 次提高到 `100` 次；`1,000,000` 累计 tokens 与 `10` 分钟墙钟门槛保持独立，达到任一边界后仍通过可审计的 `turn_continuation` Item 提示用户发送“继续”，并保留继续、压缩后继续和停止三个结构化动作。新增 99/100 次边界测试、提示契约断言和续跑卡片 E2E。
- [x] `P10-115` 用户明确要求在 k-Coder 界面增加上下文进度：输入区底栏显示紧凑百分比，详情展示最近一次 Provider 调用的 token 数与活动模型硬窗口；累计 Turn 用量与当前上下文用量使用独立的 schema v5 实时/恢复契约，Compaction 后清空旧基线，支持桌面与窄屏布局及无障碍状态。新增公共事件、存储恢复、旧索引兼容及双视口实时/压缩回归。
- [x] `P10-116` 用户明确调整优先级：增加全栈开发、质量保障和需求设计三个受限内置机器人；复用唯一 `AgentRuntime`，由后端版本化定义和追加式状态机真实注入角色与当前节点，仅允许 `complete_workflow_node` 携带有界证据推进。项目/Craft、mailbox、恢复、停止、设置页和双视口交互均已实现并验收。
- [x] `P10-117` 用户明确要求把输入区图片按钮替换为“添加”菜单：附件复用既有图片选择链路，Skill 唤起已有候选并保持独立会话能力收缩，MCP 直达专用配置；插件、小程序和 Workflow 只进入明确的“尚未接入”设置页，不伪造运行时能力。菜单支持视口约束、键盘导航、焦点恢复、桌面/窄屏与深色模式。
- [x] `P10-118` 用户明确修正品牌方案：标题栏恢复为主题感知的 `K` 品牌标记，新会话中央使用独立的“指令脉冲”欢迎图形，Tauri 原生应用图标改为 CodeBuddy 品牌蓝底白色 `K`，所有平台尺寸从同一个 SVG 母版生成；Windows ICO 纳入 Cargo 重建依赖，保留既有欢迎层级、会话状态、输入工作流和运行时契约，并通过 desktop/narrow、图形角色、图标母版、嵌入资源与真实任务栏核验。
- [x] `P10-119` 用户明确要求按照本地 Codex 插件宿主设计开发：从用户数据目录发现带 `.codex-plugin/plugin.json` 的本地插件，默认禁用并由设置页显式启停、确认删除；宿主按索引按需读取 Skill 与资源，把兼容 MCP 配置映射到既有隔离运行时，未知组件降级诊断而不执行。所有路径、链接、文件大小、遍历数量和工具命名均关闭失败，启停/删除以代际门闩即时撤销旧 Turn 持有的 MCP 能力并重建唯一工具注册表；新增版本化 IPC、审计、响应式插件管理界面及安全、生命周期、桌面/窄屏回归。
- [x] `P10-120` 用户优先新增 Windows 无安装包构建部署批处理：从任意工作目录进入仓库根目录，执行 `pnpm tauri build --no-bundle`，仅在构建成功后调用既有本地配置驱动的部署脚本，构建失败时返回非零状态且不部署旧产物。
- [x] `P10-121` 用户反馈对话输入区底栏控件挤在一排且视觉层级混乱：把项目与机器人收拢为全宽上下文带；添加、模式、推理和模型组成安静的本轮配置区，审批移到用量与发送动作一侧；统一 32px 控件节奏和菜单指示，在桌面、700px、用户截图对应的 596px 与 375px 宽度保持单行操作、稳定截断且不重叠。
- [x] `P10-122` 用户反馈同一 Patch 失败后重试成功仍显示历史失败：前端按连续工具组和补丁目标文件集合识别成功覆盖，只隐藏同目标成功调用之前的失败行、失败组状态和对应可见操作数；不同目标、最终失败、尚未成功及目标无法解析的调用继续显示，Tool Item、JSONL、Provider 历史和工具结果保持完整。
- [x] `P10-123` 用户确认输入区顶部快捷操作布局，并要求调整上下文选择器层级：文件引用、图片、Skill、更多操作和机器人选择器位于输入框上方；项目选择器移至下方配置栏并排在 Craft 模式之前。独立会话继续禁用项目专属操作，窄屏将项目选择器收束为图标以避免横向溢出，菜单、键盘焦点和审批提示保持现有行为。
- [x] `P10-011` 用户明确调整优先级：添加会话级"请求批准 / 完整访问"模式；完整访问只自动批准已注册工具，不扩大工作区或运行时边界，并保留逐次审计。
- [x] `P10-012` 用户明确调整优先级：参考 Codex 源码将已结束 Turn 的执行过程默认折叠，保留独立最终回答、完整可展开时间线和真实 Turn 总耗时；活动 Turn 使用随阶段变化且保持展开的紧凑状态标题展示动态动作与实时耗时，不显示冗余头像或固定的"正在执行"，尚无工具活动时只保留紧凑单行占位；界面折叠不改变模型上下文或 Compaction。
- [x] `P10-013` 用户明确调整优先级：把文件树改为紧凑字号；单击 UTF-8 文本文件后用本地 Monaco Editor 提供宽面板、最大化、行号、语法高亮、编辑和快捷保存，并通过工作区路径校验、SHA-256 乐观锁和共享编辑锁拒绝越界或并发覆盖。
- [x] `P10-014` 用户明确调整优先级：活动 Turn 持续展开已经收到的安全思考摘要；Gemini 推理请求启用官方 `includeThoughts` 摘要，并只投影标记为 `thought` 的文本，继续拒绝 Chat Completions 非标准原始推理和 Anthropic 原始 thinking 块。已完成摘要的默认展开规则后由 `P10-044` 调整为收起。
- [x] `P10-015` 用户明确调整优先级：把 `read_file` 默认返回上限缩为 64 KiB，新增与旧字节范围兼容的行范围契约；审计界面使用懒加载只读 Monaco 原样展示模型实际收到的读取结果、语法高亮和真实起始行号，不再二次截断。
- [x] `P10-016` 用户明确调整优先级：工作台文件/Git 面板展开时保持独立右列；对话消息和输入区限制在中间列，底部控件可响应式换行，发送/停止按钮始终可见且不被工作台遮挡。
- [x] `P10-017` 用户明确调整优先级：缩窄工作台文件/Git 右栏，常规桌面限制为 360–440px，中等窗口限制为 320–400px，在保留代码编辑和 Git 操作可用性的同时把更多宽度还给对话区。
- [x] `P10-018` 用户明确调整优先级：修复扩展刷新覆盖高级工具风险元数据导致 `search_repository` 被工作区策略拒绝的问题；扩展风险只按实际注册处理器合并且不得覆盖内置策略，失败活动优先展示真实后端错误。
- [x] `P10-019` 用户明确调整优先级：`read_file` 活动摘要在运行时显示请求行范围，完成后改用后端实际返回范围，让同一文件的并行分段读取可直接区分。
- [x] `P10-020` 用户明确调整优先级：完全移除普通 Turn 的 Provider 迭代数和工具调用总数硬上限；Goal 与子智能体默认不设 Token 预算，只执行显式预算；前端持续展示累计 Token 消耗，并以 Compaction、取消、重复调用和无进展检测终止失控循环。
- [x] `P10-021` 用户明确调整优先级：活动 Turn 中从输入区发送的新消息只进入可展开队列，不提前显示为对话气泡；队列项提供独立的立即发送和删除操作，立即发送会打断当前 Turn，并在持久化终态到达后执行该消息。
- [x] `P10-022` 用户明确调整优先级：恢复或重试会话时，在每次 Provider 请求前按调用 ID 修复持久化工具历史；只发送完整配对的工具组，剔除缺失、重复、错配及孤立结果，避免中断落盘产生无效协议序列。
- [x] `P10-023` 用户明确调整优先级：修复模型同时填充行范围和字节范围时 `read_file` 持续失败的问题；存在 `startLine/lineCount` 时以行范围为准并忽略冗余的 `offset/limit`，保留旧字节范围兼容和重复循环保护。
- [x] `P10-024` 用户明确调整优先级：把时间线上连续的思考摘要合并为单个可折叠展示组，只保留一次图标、标题和边界，组内按事件顺序展示各段内容，非思考事件继续作为分组边界。
- [x] `P10-025` 用户明确调整优先级：审批请求和解决事件按对应 `toolCallId` 插入工具活动之前，保证"审批已完成、工具仍执行中"不会在时间线上倒置；历史恢复沿用同一排序规则，并让不同完成状态的思考摘要保持独立分组。
- [x] `P10-026` 用户明确调整优先级：活动 Turn 的"思考中"等动态状态显示在过程内容之后，始终跟随最新时间线记录且不显示上下箭头；终态总耗时摘要继续位于折叠执行过程的顶部。
- [x] `P10-027` 用户明确调整优先级：对话时间线工具活动直接以"动词 + 文件名 + 行号"单行摘要展示——`read_file` 显示 `读取 <path> L<start>-<end>`，`search_repository` 显示 `搜索 <query>`，`list_directory`、`write_file`、`apply_patch`、`run_command`、`browser_navigate` 同理；失败工具仍优先显示后端错误而非参数，状态面板最近活动保持同一摘要格式。
- [x] `P10-028` 用户明确调整优先级：读取文件活动只展示路径、范围和执行状态摘要，移除下方"查看读取内容"入口及只读 Monaco 预览；读取结果仍保留在 Provider 历史和持久化工具事件中，补丁与写入内容详情不受影响。
- [x] `P10-029` 用户明确调整优先级：并行工具按实际完成顺序持久化并发布结果，避免前序慢命令让已结束的短命令持续显示"执行中"；命令进程退出、超时或取消后的 stdout/stderr 读取采用有界收尾，派生进程遗留句柄不能无限阻塞终态。并发调度策略后由 `P10-033` 按用户要求改为串行。
- [x] `P10-030` 用户明确调整优先级：Turn 进入取消或失败终态时收口同一 Turn 内仍为运行中的工具；实时事件、旧快照归一化和 JSONL 恢复使用一致的 cancelled/failed 状态、结束时间与固定耗时，不再在"Turn 已取消"后继续显示旋转图标和递增计时。
- [x] `P10-031` 用户明确调整优先级：移除活动 Turn 在尚无工具或时间线条目时显示的"等待工具调用…"空占位行，仅保留紧凑状态标题。
- [x] `P10-032` 用户明确调整优先级：消息队列没有待处理消息时不渲染"队列 (0)"容器；只有用户重复发送、产生待处理队列项时才显示队列入口，已完成或失败的历史队列项不单独触发展示。
- [x] `P10-033` 用户明确调整优先级：同一次 Provider 响应中的工具调用按返回顺序逐个执行；前一项完成并持久化结果后才启动下一项，取消或致命失败时不启动剩余工具但仍写入配对的跳过结果，避免命令、Git、搜索和审批并发争用导致界面长时间停留在执行中。
- [x] `P10-034` 用户明确调整优先级：修复命令进程已经退出但工具完成等待没有返回的问题；会话等待在通知之外定期复核真实终态，并把 `corepack npm/pnpm/yarn` 包装的构建与测试命令纳入既有低风险分类，避免无意义审批和后续串行工具被阻塞。
- [x] `P10-035` 用户明确调整优先级：修复合法 `SKILL.md` 混用 CRLF/LF 或带 UTF-8 BOM 时被误报为前置元数据无效、进而阻断消息发送的问题；分隔符改为按独立行识别且只剥离文件开头的标准 BOM，真正缺失 frontmatter 或闭合行的 Skill 继续关闭失败；Windows `canonicalize` 产生的 `\\?\` 前缀仅用于内部路径处理，不再出现在扩展诊断和错误文案中。
- [x] `P10-036` 用户明确调整优先级：为 CodeBuddy 皮肤补充独立浅色语义令牌，保留深色方案并让全局外观切换、原生控件和 Monaco 编辑器正确跟随；新增本地恢复与深浅切换 E2E 回归。
- [x] `P10-037` 用户明确调整优先级：把同名同参工具重复保护从整个 Turn 的累计次数改为连续调用次数；连续第三次调用仍关闭失败，经过不同工具调用后的合法状态复查允许执行，交替但无进展的循环继续由既有进展窗口保护终止。
- [x] `P10-038` 用户明确调整优先级：只保留 CodeBuddy 外观，删除 Paper、Midnight、VSCode 和 Amber 皮肤的定义、选择器与设置页选择 UI；界面固定使用 CodeBuddy 风格并保留深/浅模式切换。
- [x] `P10-039` 用户明确调整优先级：失败或取消后的重试继续创建独立 Turn 以保留审计与恢复边界，但实时和恢复投影都锚定原用户消息；同一用户请求的多次尝试归并到一个助手回复容器，只显示一次 `k-Coder` 角色标识，不再把重试视觉上拆成新回复。
- [x] `P10-040` 用户明确调整优先级：绑定到项目的会话只展示在侧边栏"项目"tab 的项目分组中，不再出现在"会话"tab 的普通会话列表（含搜索结果）；未绑定项目的独立会话保持原样，首帧惰性加载线程-项目映射避免项目会话在普通列表短暂闪现。
- [x] `P10-041` 用户明确调整优先级：把活动 Turn、消息队列执行锁和取消去重状态按线程隔离；同一会话仍保持单 Turn 与 FIFO，新建或切换到其他会话后可以立即并行启动独立 Turn，后台事件和队列项不得污染当前会话投影。
- [x] `P10-042` 用户明确调整优先级：修复七项对话异常语义缺陷；重试持久化并恢复原 AgentMode、复用共享 `UserInputManager`，致命工具/审计/存储错误补写失败终态，四类 Provider 将 Token 截断、内容过滤和其他非正常停止映射为失败；Provider 重试仅允许发生在未产生可见或持久化输出的尝试，并累计失败尝试用量；跨线程队列不得污染当前会话，串行工具恢复区分 Pending 与 Running。
- [x] `P10-043` 用户明确调整优先级：参考 Codex 将对话时间线上连续的工具活动聚合为可展开摘要；纯命令组显示"运行了 N 个命令"，混合工具组显示"执行了 N 个操作"，完成后默认折叠，运行中、等待、失败或取消组保持展开；审批、变更、思考和说明继续切断分组，展开后保留逐项参数、输出、状态与耗时。
- [x] `P10-044` 用户明确调整优先级：把模型上下文、模型用量、自动批准、审批结果、Compaction、文件变更和其他带详情的时间线事件统一渲染为可折叠步骤；完成的思考摘要在活动 Turn 中也默认收起，进行中的思考和工具继续展开；助手阶段性说明与最终回复保持直接可见，展开步骤后仍可检查完整详情。
- [x] `P10-045` 用户明确调整优先级：Craft 修改任务必须使用文件工具，不得把完整代码、Patch 或 unified diff 当作普通回复持续输出；停止按钮与取消命令统一使用按线程活动表，请求后显示"正在停止"，IPC 超时恢复为可重试状态。
- [x] `P10-046` 用户明确调整优先级：参考 Codex 统一对话活动摘要文案；单个纯命令组保留"运行了命令"，其他工具组显示"运行了多个命令"，文件变更步骤显示"编辑了文件"，修改类文件明细显示"已编辑 文件名"，实时事件与刷新恢复保持一致。
- [x] `P10-047` 用户明确调整优先级：收紧对话输入区正文与占位文字字号；桌面应用限制为单实例，重复启动时不创建第二个客户端，恢复并聚焦已有主窗口并显示明确提示。
- [x] `P10-048` 用户明确调整优先级：参考 Codex 强化多步骤 Turn 的阶段性进度说明，在工具批次之间展示已确认事实和下一步但不暴露私有思维链；自动 Compaction 落盘后实时进入当前时间线；纯 `run_command` 与读取、搜索、编辑等普通工具使用准确的"命令/操作"分组文案。该任务当时记录的结构化直启契约已由 `P10-049` 取代。
- [x] `P10-049` 用户明确调整优先级：参考 Codex 把模型可见的 `run_command` 改为命令文本契约；Windows 默认使用 PowerShell 并在不可用时回退到 `cmd.exe`，Unix 使用受支持的用户默认 shell；保留内部结构化进程运行时、策略审批、工作区、超时、取消、进程树清理、输出脱敏和旧历史展示边界。
- [x] `P10-050` 用户明确调整优先级：保持 Provider、工具、持久化和队列全速处理，在前端按积压量自适应展示助手正文；运行时终态到达后先排空已接收正文，再切换完成折叠。对话仅在视口贴近底部时随内容增长跟随，用户向上滚动、触摸或拖动滚动条后暂停，回到底部时恢复，不能在完成瞬间强制跳底。
- [x] `P10-051` 用户明确调整优先级：文件/文档预览使用覆盖窗口内容区的模态遮罩，预览弹窗相对整个窗口居中，不再固定在工作台右侧列；标题栏保持可见，窄屏和桌面尺寸均使用同一套响应式边界。
- [x] `P10-052` 用户明确调整优先级：在消息输入框中以 `@` 搜索并插入工作区相对文件引用，以 `/` 搜索并插入已发现的 Skill；文件搜索限制遍历规模并拒绝符号链接/目录联接逃逸，显式 Skill 选择仍由后端启用状态决定；同时修复深色模式下 Plan/Ask 提问卡片正文与选项文字对比度。
- [x] `P10-053` 用户明确调整优先级：命令或自动操作完成后，保留同一时间线折叠节点并使用自然的内容高度缓出收缩，避免终态切换时闪烁；箭头同步放慢，减少动态效果设置继续即时收起。
- [x] `P10-054` 用户明确调整优先级：流式正文尚未揭示完成时，后到的工具、事件和后续正文不得提前显示；展示层按时间线顺序逐段排空正文并依次放行后续项目，运行时事实处理、持久化和队列调度继续不受阻塞。
- [x] `P10-055` 用户明确调整优先级：新增随 Tauri 安装包发布的只读内置 Skill 层，按"内置、全局、项目"从低到高确定性覆盖并纳入扩展修订指纹；设置页明确显示内置来源，首个低风险 `workspace-review` Skill 随应用打包，Skill 内容继续不能扩大工具权限。
- [x] `P10-056` 用户明确调整优先级：保持单工作区运行时，为每个新会话持久化不可变的规范工作区绑定；项目会话创建、选择、启动恢复及发送前先同步真实活动工作区，旧会话首次执行时绑定当前工作区；普通 Turn、重试和 Compaction 在模型调用或工具执行前拒绝会话与活动工作区不一致，避免项目分组与全局工作区错位后读写另一个仓库。
- [x] `P10-057` 用户明确调整优先级：用户消息角色行提供复制按钮，把原始消息文本写入剪贴板并显示短暂成功反馈；没有文本的纯附件消息不显示复制入口。
- [x] `P10-058` 用户明确调整优先级：命令参数详情改用按需加载的只读 Monaco，按实际 PowerShell、CMD 或 Unix Shell 显示语言、行号和高亮；活动、失败或取消 Turn 保持当前思考、计划、工具和详情步骤展开，只有 `turn_completed` 成功终态才自动折叠执行过程。
- [x] `P10-059` 用户明确调整优先级：`apply_patch`、`write_file` 和已应用文件变更详情改用按需加载的只读 Monaco；已应用变更使用持久化前后快照展示按文件语言高亮的内联 Diff、增删统计和复制入口，快照缺失时降级展示已持久化 unified diff，不重新读取工作区文件。
- [x] `P10-060` 用户明确调整优先级：按 Codex 的 `Thread -> Turn -> Item` 契约演进对话运行时；第一阶段让助手正文增量、工具前说明持久化记录和最终消息共享运行时生成的稳定 Item ID，前端按 Item ID 投影并保留旧事件显示兼容，为统一 item lifecycle 和真正的 steer/interrupt 建立基础。
- [x] `P10-061` 用户明确调整优先级：新增 schema v3 的 `item_started`/`item_completed` 公共事件和 schema 7 的 JSONL 事实事件，先覆盖助手回复 Item；运行时在成功、失败和取消终态前闭合稳定 Item，前端接管流式助手消息身份并保持旧 schema 显示兼容，为后续推理、工具、审批和变更 Item 生命周期迁移提供统一边界。
- [x] `P10-062` 用户明确调整优先级：为安全 Reasoning 摘要复用统一 Item 生命周期；首个增量或直接完成均先启动 `reasoning` Item，重复完成事件去重，摘要超限、Turn 失败和取消闭合未完成 Item，私有 thinking 和摘要正文继续不进入 Provider 历史。
- [x] `P10-063` 用户明确调整优先级：为 Tool call 复用统一 Item 生命周期；每个已接受调用使用 `ToolCall.id` 启动 `tool` Item，工具开始、stdout/stderr 增量和结果共享该身份，成功/失败/取消均写入确定性 `item_completed`；JSONL 恢复以生命周期状态区分 pending、running、completed、failed、cancelled，前端继续沿用 ToolActivity 投影。
- [x] `P10-064` 用户明确调整优先级：为 Approval、Change、UserInput 和 ContextCompaction 复用统一 Item 生命周期；分别使用请求 ID、变更 ID 和压缩事实事件 ID，成功、失败、取消均确定性闭合；审计失败回滚后关闭 Change Item，崩溃恢复只关闭实际启动的 Item，缺少启动事实的旧会话不伪造完成事件。
- [x] `P10-065` 用户明确调整优先级：从 JSONL 统一投影版本化 `Thread -> Turn -> Item` 历史，新增 turns/items 有界双向游标读取；工作台从 Item 页投影消息、时间线、交互和变更并可加载更早 Turn，旧 `read_thread` 仅作兼容降级。
- [x] `P10-066` 用户明确调整优先级：把阻塞式 `run_turn` 拆出异步 `turn_start` 和版本化 TurnHandle；运行时在 `turn_started` 已持久化并发布后立即返回稳定 `turnId`，后台继续唯一 AgentRuntime 循环，正常前端只通过事件与统一历史查询获取结果，阻塞入口保留迁移兼容。
- [x] `P10-067` 把每线程单活动 Turn、FIFO 输入和控制唤醒迁入 Rust Thread mailbox；复用主 AgentRuntime、工作区互斥、取消令牌与 JSONL 事实，前端退出调度职责。
- [x] `P10-068` 实现带 `expectedTurnId` 前置条件的真正 `turn_steer` 与精确 `turn_interrupt`；steer 不得退化为取消后排队，过期控制请求必须拒绝。
- [x] `P10-069` 完成 Thread fork/resume/rollback 与协议兼容收尾；分支和回滚保留审计边界，旧 schema 与阻塞入口具有明确迁移或淘汰策略。
- [x] `P10-070` 用户要求继续优化 Codex 对话差异：重试通过 Rust Thread mailbox 和异步 `turn_retry` 返回稳定 handle，队列区分普通消息与重试；桌面停止及错误恢复只使用带精确 `turnId` 的 `turn_interrupt`，不再调用线程级 `cancel_turn`，旧阻塞命令仅保留迁移兼容。
- [x] `P10-071` 用户要求继续优化 Codex 对话差异：普通 pending 消息通过单一 `turn_steer_queued` 命令从 Rust mailbox 原子接纳到精确活动 Turn；正文与附件以后端队列为准，只有 `TurnControl` 接受后才删除队列项，worker 抢先、Turn 收尾、重复点击和 retry 项均不得造成重复注入或消息丢失，前端不再组合 steer 与独立删除请求。
- [x] `P10-072` 用户明确把候选 A-G 全部提升为实施任务：建立按 thread 的生命周期 operation gate，统一串行化 Turn 接纳、mailbox worker 取项、fork、rollback 和恢复边界；worker 出队后持有门闩直到 active Turn 注册，fork/rollback 对 active 或 pending mailbox 明确拒绝，并新增确定性并发回归。
- [x] `P10-073` 把进程级 JSONL append 锁替换为按 thread 的有界持久化 writer；同线程严格保序、跨线程并行，状态事件继续在 durable ack 后发布，退出和失败必须有界收尾。
- [x] `P10-074` 建立可从 JSONL 重建的 SQLite 增量 Turn/Item 索引；append 不再整线程 delete/reinsert，历史分页按索引读取并保持 rollback/fork、旧 schema 和游标拒绝语义。
- [x] `P10-075` 为 mailbox snapshot 增加 revision 和版本化 change 通知，前端按事件/快照收敛队列与活动 Turn，移除固定 500ms 终态刷新和重复活动状态来源。
- [x] `P10-076` 新增脱敏的结构化 `TurnError`、retryability 和显式 Turn/Item 运行状态；旧字符串事件继续兼容，工作台不得解析错误文案决定操作。
- [x] `P10-077` 沿 A-E 形成的职责边界拆分 thread operations、storage writer/index、command handlers 和 frontend reducers；保持公共协议和 JSONL 事实不变，不进行大爆炸式重写。
- [ ] `P10-078` 精简 Monaco ESM 与 worker 加载；初始工作台和非 TypeScript 只读预览不得加载 TypeScript worker，保持现有编辑、Diff、行号、高亮和双视口交互。
- [x] `P10-079` 用户优先修复设置弹窗关闭后的侧栏视图回退：从项目视图打开设置并关闭后，继续停留在项目视图。
- [x] `P10-080` 用户优先修复线程切换期间的视图状态残留：异步读取目标线程历史时立即清空上一线程的消息、时间线、计划、目标和队列投影，并新增延迟读取回归测试。
- [x] `P10-081` 用户明确询问并调整命令展示：`run_command` 在对话工具项中直接显示原始命令文本、实际 Shell、工作目录和超时；Shell 名称优先读取工具结果元数据，缺失时显示通用 `Shell`，不把 PowerShell 误报为 CMD；同步桌面命令时间线回归断言。
- [x] `P10-082` 用户明确调整优先级：工作台新增"终端"标签，基于既有宿主 PTY 命令与本地打包的 xterm.js 提供交互终端；`start_pty` 空 `program` 表示平台默认交互式 Shell（Windows 依次 `pwsh` → Windows PowerShell → `cmd.exe`，Unix 使用用户默认 shell，均初始化 UTF-8 编码），工作目录固定为当前工作区根目录；终端会话在标签切换间保持，进程退出后显示终态并可重启。
- [x] `P10-083` 用户明确要求优化长 Turn 失控与上下文滚雪球：普通、无活动 Goal 的根 Turn 每 `30` 次 Provider 请求、`1,000,000` 累计 tokens 或 `10` 分钟进入可审计的分段续跑门，支持继续、压缩后继续和停止且不恢复生命周期硬上限；自动 Compaction 阈值降至 65%，只保留最近 2 个工具结果，超过 4 KiB 的输出改为确定性摘要。新增 ADR 0031、Rust 运行时/公共契约/压缩回归和 desktop/narrow 续跑卡片 E2E。默认请求门槛后由 `P10-114` 调整为 `100` 次。
- [x] `P10-084` 用户优先修复会话重读后的重复助手回复：历史恢复与实时事件产生同一 Turn 的重叠消息投影时，按稳定 Turn 身份归并为单条助手消息；缺少 `turnId` 的历史消息可从同 ID 时间线文本恢复归属，并优先保留已完成且有正文的最终投影。新增恢复缺失 Turn 归属和同 Turn 多助手投影的 desktop/narrow 回归。
- [x] `P10-085` 用户优先改造 Agent 对话失败态：Turn 顶层只显示一次“请求未完成”及耗时/操作数，展开区直接显示错误原因和就地重试，不再把 `turn_failed`/`turn_completed` 终态重复渲染为内部步骤；Turn 失败不再同时弹出全局错误条，带终态时间线的消息不再追加“生成失败/已停止”，旧无时间线消息继续兼容。过程、工具输出和持久化终态事实保持完整，并新增深色 desktop/narrow 失败态、折叠和重试回归。
- [x] `P10-086` 用户优先参考 Codex 增加多层折叠：完成、失败和取消 Turn 都默认折叠为单行摘要；展开外层后，错误原因和就地操作直接可见，计划、思考、连续工具组、生命周期步骤及命令输出仍保持独立折叠。活动 Turn 只自动展开未完成思考、进行中计划和 pending/running 工具，工具完成、失败或取消后立即收起。实时与历史投影继续共享同一事实时间线，折叠不删除 JSONL、Provider 历史或工具输出。
- [x] `P10-087` 用户优先优化对话可见信息：Provider 上下文、逐次模型用量、审批请求/解决和自动批准事件继续持久化与恢复，但不再显示为过程步骤；手动待审批只保留可操作卡片，用量集中到设置页，输入区默认审批模式缩为图标且完整访问持续显示文字状态。会话标题不再重复显示 Token，纯审计孤立 Turn 不渲染空助手容器，终态或非活动 Turn 的旧未闭合思考不再显示“生成中”，并覆盖 desktop/narrow 可见性回归。
- [x] `P10-088` 用户优先优化思考内容展示：短小且只描述规划、准备、检查、运行、测试等当前动作的 reasoning 过程句不再渲染原文，活动 Turn 仍显示紧凑“思考中”状态；包含结论、原因、风险、约束或足够上下文的安全摘要改称“思考摘要”并按既有规则折叠。所有摘要事件继续持久化和恢复，不显示原始推理、系统提示或模型指令，并覆盖 desktop/narrow 活动与完成状态回归。
- [x] `P10-089` 用户优先调整对话摘要图标：“思考摘要”使用灯泡图标，完成态“执行了 …”使用勾选圆图标；运行中、失败和取消状态继续使用各自状态图标，并覆盖 desktop/narrow 图标回归。
- [x] `P10-090` 用户优先调整普通操作组图标：完成态“执行了操作/执行了多个操作”使用深色扳手图标，与“运行了命令”的深色终端图标保持同一视觉语系；运行、失败和取消继续使用各自状态图标与状态色，并覆盖 desktop/narrow 图标及主题墨色回归。
- [x] `P10-091` 用户反馈“画图连不上”：Gemini 图像生成模型改用 `generateContent` 单次请求并声明 `TEXT/IMAGE` 响应，跳过不兼容的工具与推理字段；解析有界 `inlineData` 为助手图片内容，持久化并在会话中展示，普通 Gemini 文本流保持原协议。
- [x] `P10-092` 用户优先要求在对话输入区选择项目并明确当前工作区：选择器以后端项目注册表和当前会话工作区绑定为准，支持搜索最近项目和从本地目录新建项目；切换已有项目时先同步真实活动工作区，再创建不可变绑定到目标项目的新会话，活动 Turn 期间禁止切换，并覆盖 desktop/narrow 菜单、切换顺序和布局回归。
- [x] `P10-093` 用户反馈活动任务切换会话后像是重新开始：工作台显式标记从历史或 mailbox 恢复的活动 Turn，使恢复快照中的已有正文首帧完整可见，仅让后续实时增量继续按节奏展示；保持每会话独立运行状态和既有 mailbox 调度，不因选择会话调用启动或重试命令，并覆盖 desktop/narrow 切换恢复回归。
- [x] `P10-094` 用户反馈项目选择器泄漏 Windows `canonicalize` 产生的 `\\?\` 路径前缀：前端统一在展示、搜索和项目路径比较边界还原本地盘符与 UNC 用户路径，项目记录、会话绑定、工作区切换参数及后端授权继续保留原始规范路径；覆盖 desktop/narrow 本地与 UNC 展示、可见路径搜索和原始切换参数回归。
- [x] `P10-095` 用户要求侧栏工作区项目与输入区项目选择联动：两处共用由持久化会话绑定、显式添加项目和当前工作区派生并按规范路径去重的项目集合，后端最近目录只补全匹配记录；侧栏显示与输入区勾选一致的活动项目，输入区切换项目和侧栏切换项目会话均从同一活动会话工作区双向更新，并覆盖 desktop/narrow 列表一致性及桌面双向切换回归。
- [x] `P10-096` 用户反馈对话图片无法预览和识别：图片内容块改用前端公共契约 `dataUrl` 序列化并兼容旧 JSONL 的 `data_url`；旧 Provider 目录迁移已确认的 GPT 多模态模型视觉能力，新建模型默认支持图片且允许文本模型显式关闭。视觉模型直接向 Provider 发送原始 Data URL 并丢弃 OCR 文本，PP-OCRv5 仅作为 `supportsVision=false` 模型的降级路径；覆盖历史预览、配置迁移、显式关闭、原图请求和 OCR 隔离回归。
- [x] `P10-097` 用户要求融合 Codex 的计划进度展示：正式 `update_plan` 计划从 Turn 执行折叠区内的展开列表改为回答后持续可见的紧凑胶囊，显示“第 X/Y 步”，并在当前 Turn 存在未撤销文件变更时显示唯一目标文件数和累计增删行；悬停、键盘聚焦或点击以顶层浮层展示各步骤和已完成、进行中、待处理、失败、已跳过状态，支持点击固定、`Escape`/点击外部关闭、窄屏收缩和减少动态效果偏好。`todo_write` 任务清单继续沿用原有独立投影，不重复显示同义进度。
- [x] `P10-098` 用户要求优化对话连续性：Compaction 摘要升级为 v2，除用户约束和近期工具结果外保留最近 4 条有界用户请求；纯图片请求保留图片数量标记，`ContextCompacted` 实时事件和历史 Item 同步携带保留数量，旧摘要缺少字段时兼容为空，避免长对话压缩后丢失当前用户意图。
- [x] `P10-099` 用户要求参考 Codex 优化连续命令组折叠：工具组摘要改为状态点、工具图标、动作标题和可选状态的轻量单行，完成态隐藏冗余“已完成”，展开内容使用左侧连接线和紧凑工具行；保留现有 `details` 交互、自动展开/收起、工具参数与输出审计能力。
- [x] `P10-100` 用户要求进一步简化命令活动：展开命令组后，`run_command` 只显示脱敏后的原始命令文本和“等待运行/运行中/已运行/运行失败/已取消”内联状态，不再渲染耗时、Shell/工作目录/超时参数卡或命令输出子折叠；工具结果、输出 chunks、JSONL、Provider 历史和 Compaction 输入保持不变，避免把界面降噪误当成模型上下文缩减。
- [x] `P10-101` 用户要求移除工具组折叠的卡片外观并约束多命令展开高度：摘要改为无边框、透明背景的轻量行，保留状态点、工具图标、箭头和展开连接线；“运行了多个命令”的内容区高度上限固定为 168px，长命令或较多命令在组内纵向滚动，单命令和普通操作组继续按内容自然展开。
- [x] `P10-102` 用户要求进一步简化工具组摘要：移除左侧终端/扳手 logo，仅保留状态点和标题；箭头紧跟标题行，非完成态状态文字跟在箭头之后，收起显示右箭头，展开显示下箭头，继续保留无卡片样式、自动收起和多命令 168px 组内滚动。
- [x] `P10-103` 用户要求在输入区左下角项目选择器增加“不在项目中”：选择后创建持久化的显式无项目会话，不切换全局工作区；该会话不绑定项目，不加载项目指令、Skill、MCP 或 Hook，从模型工具层关闭文件、仓库搜索、Patch、Shell 和子智能体，拒绝直接子智能体 IPC，并关闭 `@` 文件和 `/` 项目 Skill 输入候选；旧会话默认保持项目模式兼容。
- [x] `P10-104` 用户反馈对话工作期间频繁弹出“Windows 找不到 `\\` 文件”：工作台完成路径规范化、链接解析和越界拒绝后，只在系统打开边界把 `\\?\` / `\\?\UNC\` 派生为 Shell 兼容路径；文件打开和资源管理器定位改用 Tauri opener 的系统 API，移除 `cmd /C start` 二次解析，IPC 失败在文件预览内显示，并覆盖本地盘符、UNC、越界及 desktop/narrow 错误反馈。
- [x] `P10-105` 用户反馈输入区模式菜单只能通过再次点击底部触发器关闭：模式选择器打开时监听文档点击，点击其容器以外的任意应用区域自动关闭；触发器和菜单选项仍保持原有交互，并覆盖 desktop/narrow 回归。
- [x] `P10-106` 用户反馈一次搜索命令显示“运行失败”，并要求思考摘要移除卡片折叠且使用中文：确认该命令由 Windows PowerShell 因 `rg` 不在宿主 `PATH` 中抛出 `CommandNotFoundException`，模型随后已回退到 `Select-String`；当时增加的“不得假定 `rg` 已安装”临时兼容提示由后续 `P10-107` 内置资源替代。Provider 继续完整持久化原始安全摘要，系统提示以中英文要求简体中文，常规对话过滤纯英文或英文叙述占主导的摘要；相邻可见中文摘要统一为无边框、无底色、无状态和箭头的始终展开平铺组。
- [x] `P10-107` 用户要求参考 Codex 直接内置 `rg`：Windows x86_64 Tauri 资源固定包含官方 ripgrep 15.2.0、许可证、归档与文件 SHA-256 及可复现获取脚本；execution 层验证资源存在，并在普通命令和交互 PTY 的子进程环境中把内置目录置于 `PATH` 首位，调用方 `Path`/`PATH` 不能覆盖其优先级，工作区切换继续保留；模型工具说明明确 `rg` 可直接使用，不修改多线程宿主全局环境。
- [x] `P10-108` 用户要求按四阶段完成长 Turn Token 优化：保留模型硬上下文并拆分工作压缩基准，把系统指令和工具 Schema 纳入请求估算；CompactionSummary v3 保留当前请求和高信号工具观察并记录压缩前后估算；Runtime Metrics 汇总压缩节省；Provider 工具描述和工具结果使用有界载荷，并以 19 轮回放及完整质量门槛验证累计输入下降且不破坏工具配对、持久化和恢复。
- [x] `P10-109` 用户反馈 `read_file`、`list_directory` 和 `rg` 命令频繁失败：从实际 Turn 日志确认模型猜测不存在路径、混用文件/目录工具，以及 PowerShell 不为原生 `rg` 展开 `dist/assets/CodeEditor-*.js`。文件工具 Schema 和系统提示改为先发现精确路径；后端按预期文件类型返回有界同级候选与修复动作，继续优先拒绝越界；失败的 PowerShell `rg` 路径通配符附加 `--glob` 恢复提示；命令行在不展开完整输出的前提下显示有界失败摘要。
- [x] `P10-110` 用户继续反馈 Monaco 探查中的 `rg --files ... | rg '...\.js$'` 无输出失败：从实际 Turn 事件确认命令以退出码 1、空 stdout/stderr 结束，并在真实 Windows PowerShell 5.1 中复现原生管道被重新编码为 CRLF、导致下游 `rg` 的 `$` 行尾锚点失配。`run_command` Schema 和系统提示要求接收端使用 `rg --crlf` 或 `Select-String`；后端只对 `powershell.exe`、退出码 1、空输出、真实管道和未修复锚点的组合附加专用恢复提示，继续保留原始命令审计且不静默改写或重跑。路径通配符示例同步覆盖 `dist/assets/index-*.js`。

验收门槛：

- 干净机器能够安装、配置、更新、回滚和卸载 k-Coder。
- 不存在尚未解决的已知严重安全问题。
- 发布产物能够追溯对应源码、依赖和配置。
- 恢复流程必须实际演练，不能只存在于文档中。

交付版本：`1.0.0`。

---

## Phase 11 - 成熟智能体与 cn-codex 能力复核

目标：根据可靠证据而不是功能数量判断成熟度。

核心能力复核：

- [ ] 多 Provider 覆盖由真实用户需求和协议夹具驱动。
- [ ] 原生流式工具调用在所有支持的 Provider 上保持可靠。
- [ ] 读取、编辑、Patch、命令、终端、Git、计划、Skills、MCP 和子智能体达到生产可用。
- [ ] Compaction、持久化、崩溃恢复、取消、审批和审计历史达到生产可用。
- [ ] 对浏览器、记忆、Goal、机器人、移动端和插件作出明确的保留或延后决定。

成熟度标准：

- [ ] 任务评估覆盖仓库理解、针对性修改、调试、测试和重构。
- [ ] 安全评估覆盖路径穿越、符号链接、Prompt Injection、破坏性命令、密钥泄露和扩展滥用。
- [ ] 真实任务完成率、延迟、成本和故障恢复达到文档规定目标。
- [ ] 持续测量架构热点，在文件变成巨型运行时容器之前拆分。
- [ ] 项目具备稳定的扩展契约和迁移政策。

当核心工作流可靠且可以量化时，项目即可视为成熟；不需要复制 cn-codex 的每一项功能。

---

## 持续质量工作

以下工作贯穿每个阶段。

### 测试

- 对领域逻辑和解析器进行单元测试。
- 对 Provider 流和外部协议进行夹具测试。
- 对存储、文件系统边界和进程进行集成测试。
- 对关键工作流进行桌面端到端测试。
- 对智能体行为进行回归评估。

### 安全

- 默认拒绝工作区之外的访问。
- 策略决策由运行时掌控。
- 对密钥进行脱敏并使用安全凭据存储。
- 使用结构化审批和审计事件。
- 在发布节点进行依赖和威胁模型复核。

### 可观测性

- 使用带关联 ID 的结构化日志。
- 记录 Turn、Provider 调用、工具调用、审批和子智能体耗时。
- 限制诊断产物大小。
- 向用户显示不泄露密钥的明确失败原因。

### 文档

- 每个任务完成后更新本路线图。
- 把难以撤销的架构决策记录为 ADR。
- 保持运行时协议和数据位置说明为最新状态。
- 记录平台差异和已知限制。

## 路线图变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-17 | 完成用户优先插入的 `P10-123` 对话输入区快捷操作与上下文选择器层级调整：顶部工具栏提供文件引用、图片、Skill、更多操作和机器人选择器，默认“普通智能体”位于输入框上方；项目选择器移至下方配置栏并排在 Craft 前，375px 时保留项目图标并压缩文字，596px、700px 与桌面保持选择器、审批、用量和发送操作有界。新增文件/Skill/图片/更多入口、独立会话禁用、深色和四档几何回归；`pnpm build`、`cargo fmt --check`、`cargo check`、系统 Edge 专项 Playwright 9 passed/1 skipped 与 `git diff --check` 通过。默认并发 `cargo test` 在 121 秒时因既有并发时序波动失败后超时，失败的子智能体用例单独复跑通过；单线程全量测试在 240 秒时停在既有 `execution::tests::cancellation_terminates_descendants`，本任务未修改对应 Rust 路径。本次未声明原生桌面窗口人工验证。下一项任务仍为 `P10-078`。 |
| 2026-08-17 | 复核并修正 `P10-123` 输入区视觉回归：移除重复的项目入口约束，并将上方“普通智能体”选择器收敛为仅显示机器人图标，保留可访问名称、标题提示和菜单交互；重新生成 596px 截图确认顶部图标栏与下方单一 `k-coder` 选择器层级正确。`pnpm build`、专项 Playwright 9 passed/1 skipped 和 `git diff --check` 通过。 |
| 2026-08-17 | 继续修正 `P10-123` 的入口辨识度：标题栏“子智能体”改用 `UsersRound` 群组图标并增加专用标记，输入区下方机器人选择器继续使用 `Bot` 图标，避免两个功能入口再次被看作同一控件。 |
| 2026-08-17 | 完成用户优先插入的 `P10-122` 成功 Patch 重试展示降噪：同一连续工具组内，后续成功 `apply_patch` 按完整目标文件集合覆盖此前同目标失败，工具组与 Turn 摘要只统计可见操作；不同文件和最终失败继续显示“包含失败”与错误详情。事实事件、持久化、Provider 历史和工具结果不变。新增 desktop/narrow 实时与刷新恢复回归及截图，`pnpm build` 和专项 Playwright 2/2 通过；下一项任务仍为 `P10-078`。 |
| 2026-08-17 | 完成用户优先插入的 `P10-121` 对话输入区响应式重排与视觉打磨：项目与机器人选择器组成全宽上下文带；添加、模式、推理和模型按本轮配置分组，审批移到用量与发送/停止动作一侧；菜单控件统一为 32px 安静工具栏样式，仅保留完整访问警示色与主动作强调，596/700px 下完整访问继续显式显示警示文案。新增 desktop、700px、用户截图对应的 596px 和 375px 深色几何布局及截图回归；`pnpm build`、`cargo check`、单线程全量 Rust 354/354、系统 Edge 专项 Playwright 6/6、审批状态复跑 2/2 和 `git diff --check` 通过。默认并发 Rust 全量测试曾有 1 个既有取消时序用例波动，单测复跑与单线程全量均通过；`cargo fmt --check` 仍被本任务未修改的 `src-tauri/src/execution.rs` 既有格式差异阻塞。本次未声明原生桌面窗口人工验证。下一项任务仍为 `P10-078`。 |
| 2026-08-17 | 完成用户优先插入的 `P10-120` 无安装包构建部署批处理：新增 `scripts/build-deploy.bat`，固定从脚本位置切换到仓库根目录，跳过 MSI/NSIS bundle 和 WiX 下载，仅在 Release 构建成功后调用 `scripts/deploy.ps1`；失败分支返回非零状态并跳过部署。完成批处理静态检查和 `git diff --check`，未执行会镜像覆盖本机部署目录的真实部署。下一项任务仍为 `P10-078`。 |
| 2026-08-17 | 完成用户优先插入的 `P10-119` 本地 Codex 插件兼容宿主：新增用户数据目录插件发现、严格 manifest/组件校验、默认禁用状态、Skill 目录与资源按需读取、兼容 MCP stdio/HTTP 映射、逐插件诊断、审计及确认删除；启停和删除通过共享代际门闩、取消令牌与客户端关闭即时撤销旧 Turn 持有的 MCP 处理器，宿主扫描失败统一清空索引并重置持久启用状态。文件读取使用拒绝链接/重解析点的句柄并复核最终路径，以 `limit + 1` 执行读后上限；扫描采用插件级聚合预算，未知组件只降级显示。插件设置页支持刷新、启停、状态/能力/诊断、后端路径确认删除、窄屏与深色。`pnpm build`、`cargo fmt --check`、`cargo check`、单线程全量 Rust 353/353、插件 Playwright 6 passed/2 skipped 和 `git diff --check` 通过；全量 E2E 为 120 passed、8 skipped、6 failed，失败仍是既有的计划完成文案、恢复态执行 DOM 数量和终端启动计数三组 desktop/narrow 断言，插件专项无失败。使用隔离标识 `com.kcoder.validation.p10119plugins` 启动真实 `pnpm tauri dev`：从实际宿主根目录发现并启用测试插件，经真实 `AgentRuntime` 和本地假 Provider 调用 `plugin_skill_read` 读到标记内容；禁用后下一 Turn 的插件目录和工具均消失，700×820 深色窗口无横向溢出，重新启用后通过名称与后端路径确认删除且磁盘目录消失；假凭据未写入隔离数据目录，测试进程和端口均已停止，既有应用进程未受影响。下一项任务为 `P10-078`。 |
| 2026-08-17 | 完成用户优先插入的 `P10-118` 品牌修正：标题栏恢复为主题感知的 `K` 品牌标记，新会话中央保留独立的“指令脉冲”欢迎图形但不再把它声明为品牌 Logo；原生图标母版从绿色改为 CodeBuddy 品牌蓝 `#2F6FE4`，并通过 Tauri CLI 重新生成 Windows ICO、macOS ICNS、通用 PNG、Appx、iOS 与 Android 图标。实际发现仅更新 ICO 会复用绿色旧 `resource.lib`，因此 `build.rs` 新增 `icons/icon.ico` 重建依赖；全新路径提取的调试 exe 嵌入图标已变蓝。定向 Playwright desktop/narrow 与图标构建契约 4/4、`pnpm build`、`cargo check`、全量 Rust 325/325 和 `git diff --check` 通过；全仓 rustfmt 检查仍只报告并行新增的 `extensions/mcp.rs` 与 `extensions/plugins.rs`，本任务未改写这些并行文件。现有 `pnpm tauri dev` 工作流重建最新调试资源，真实深色 Tauri 窗口确认顶部蓝色 `K`、中央指令脉冲，Windows 任务栏按钮实拍为蓝底白色 `K`；开发服务继续监听 `http://localhost:1420`。下一项任务为 `P10-078`。 |
| 2026-08-16 | 完成用户优先插入的 `P10-116` 内置机器人工作流：新增全栈开发、质量保障和需求设计三个版本化定义，每个包含 5 个宿主节点；机器人复用唯一 `AgentRuntime`，后端每 Turn 注入实际角色、目标和当前节点，只允许 `complete_workflow_node` 以精确节点 ID、总结和 1–8 条有界证据推进，普通文本和模型提供的下一节点无效。运行实例以追加式 `runtime-data/advanced/workflows.jsonl` 恢复；项目/Craft 门禁、mailbox `workflowId`、同毫秒追加顺序、取消生命周期门闩、旧快照兼容和排队禁止 steer 均关闭失败。输入区提供机器人选择、活动进度与停止，设置页展示定义，活动或排队时锁定机器人与模式，独立会话禁用。新增设计、ADR、Rust 与 desktop/narrow E2E；专项 Playwright 8/8 通过。`pnpm build`、Rust 格式检查、`cargo check` 和 `git diff --check` 通过；并行 Rust 全量受既有多智能体 `<350ms` 时序断言影响为 314/315，失败项单独复跑通过，单线程全量 315/315 通过。全量 E2E 为 109 passed、6 skipped、7 failed；其中 Monaco 双视口用例在清理遗留 Vite 后 2/2 复跑通过，其余 6 条仍是既有的计划完成文案、恢复态执行 DOM 数量和终端启动计数三组 desktop/narrow 断言。使用隔离标识 `com.kcoder.validation.p10116robots`、端口 1428/9336 启动 `pnpm tauri dev`；真实 Tauri WebView 直接调用后端确认 3 个定义与各 5 个节点，并验证 4 项选择菜单、质量保障选择、Craft 锁定、3 个设置页条目和无横向溢出，未发起外部模型请求。下一项任务为 `P10-078`。 |
| 2026-08-16 | 完成用户优先插入的 `P10-117` 输入区“添加”菜单：原图片按钮改为加号入口，统一提供添加附件、Skill、插件、MCP、小程序和 Workflow 六项；附件继续调用原生图片选择器，Skill 打开真实候选并可插入输入框，MCP 直达专用 JSON 配置页；尚无运行时契约的插件、小程序和 Workflow 只进入明确的“尚未接入”页。菜单使用 Portal 和视口夹取定位，支持 Esc、Home/End、方向键、焦点恢复、独立会话禁用 Skill、窄屏和深色模式。定向 Playwright 为 5 passed、1 skipped；最终共享源码通过独立端口 1428 完整运行的 E2E 为 110 passed、6 skipped、6 failed，失败仍是既有的计划完成文案、恢复态执行 DOM 数量和终端启动计数三组 desktop/narrow 断言。`pnpm build`、Rust 格式检查、`cargo check` 和 `git diff --check` 通过；默认并行 Rust 全量在该轮因既有多智能体 `<350ms` 时序断言抖动为 312/313，失败项单独复跑通过，最终稳定源码的单线程全量 315/315 通过。使用隔离标识 `com.kcoder.validation.p10116`、端口 1427/9335 启动 `pnpm tauri dev`；真实 Tauri WebView 验证六项菜单、Skill 候选与选择、MCP/三类占位路由、700x820 窄视口和深色主题，附件入口实际打开原生“打开”对话框后取消；并行加入内置机器人选择器后再次确认底栏换行和菜单边界，未写入扩展配置或发起外部模型请求。下一项任务为 `P10-078`。 |
| 2026-08-16 | 完成用户优先插入的 `P10-115` 输入区上下文进度：输入框底栏新增固定宽度的仪表百分比，点击后显示最近一次 Provider 调用 token、活动模型硬窗口、进度条和剩余额度；70%/90% 提供警告/临界状态，未配置模型或 Compaction 后显示等待更新。事件 schema 升至 v5，`usage_updated` 明确分离 Turn 累计 `usage` 与单次 `contextUsage`；线程详情、统一历史及 SQLite 历史索引恢复最近调用用量，旧索引缺失字段兼容为空，Compaction 清空旧基线。新增公共序列化、存储/压缩、旧索引兼容测试及 desktop/narrow Playwright 回归（2/2），截图确认弹层有界且不与发送按钮重叠；`pnpm build`、Rust 格式/检查和 `git diff --check` 通过。默认并行 Rust 全量受既有多智能体 `<350ms` 时序断言抖动影响为 304/305，该项单独复跑通过，单线程全量 305/305 通过。使用隔离标识 `com.kcoder.validation.p10115`、端口 1426/9334 启动 `pnpm tauri dev`，真实 1200×760 Tauri WebView 加载最新页面并验证触发器、弹层、等待态和无横向溢出，未发起外部模型请求。下一项任务为 `P10-078`。 |
| 2026-08-16 | 完成用户优先插入的 `P10-114` 普通 Turn 续跑门槛调整：无活动 Goal 的根 Turn 默认请求门槛从 30 次提高到 100 次，1,000,000 累计 tokens 与 10 分钟墙钟门槛保持不变；第 100 次请求完成后的安全边界生成可审计 `turn_continuation`，提示明确“请发送‘继续’”，并沿用继续执行、压缩后继续和停止执行三个动作。新增 99 次不触发、100 次触发的 Rust 回归及提示契约断言，续跑卡片 desktop Playwright 1/1 通过；`pnpm build`、Rust 格式/检查、全量 Rust 测试（302/302）和 `git diff --check` 通过。使用隔离标识 `com.kcoder.validation.p10114`、端口 1425/9333 启动 `pnpm tauri dev`，真实 Tauri WebView 经本地假 Provider 确认前 99 次无续跑请求、第 100 次进入 `awaiting_approval`，点击“继续执行”后第 101 次请求完成；未发起外部模型请求。当前工作返回正在进行的 `P10-115`。 |
| 2026-08-16 | 完成用户优先插入的 `P10-113` 重复 Compaction 请求身份修复：从实际会话确认 Turn 均以成功终态结束，停止来自 v3 摘要被重新插入为 `User` 后反复压缩，最终覆盖并截断真实请求。运行时改为分别保存结构化摘要、压缩后消息和只由 JSONL `UserMessage` 累积的权威用户上下文；CompactionSummary 升级 v4，重复压缩直接合并结构化字段，旧 v3 递归摘要在内存中清理请求、近期请求和受污染约束，原始日志不重写。新增 16 次压缩回放和持久化旧会话恢复测试。`pnpm build`、Rust 格式/检查、全量 Rust 测试（301/301）和 `git diff --check` 通过；使用隔离标识 `com.kcoder.validation.p10113`、端口 1425/9333 启动 `pnpm tauri dev`，真实 Tauri WebView 经本地假 Provider 完成原始请求及“继续”两个 Turn，18 次 Provider 请求触发 12 个压缩 Item，所有请求均保留真实用户意图且每次最多一个摘要头，未发起外部模型请求。下一项任务仍为 `P10-078`。 |
| 2026-08-16 | 完成用户优先插入的 `P10-112` MCP 专用 JSON 配置与设置界面：新增全局/项目 `mcp.json`、确定性四层合并、严格未知字段/重复 ID/命令/URL/凭据校验、同目录临时文件替换、路径规范化与链接逃逸拒绝；设置页按 UI skill 形成双作用域 JSON 直接编辑、格式化、即时错误反馈、真实诊断、系统凭据和 Hooks 工作流，不维护重复的可视化表单状态，并在 375/768/1024/1440 断点保持有界。新增 ADR 0038、扩展文档、安全单测和 desktop/narrow E2E；`pnpm build`、`cargo fmt --check`、`cargo check`、单线程全量 Rust 测试（302/302）、MCP Playwright（4 passed、2 个重复项目跳过）及 `git diff --check` 通过。使用隔离标识 `com.kcoder.validation.p10112`、端口 1424/9224 启动 `pnpm tauri dev`，真实 Tauri WebView 保存并回读 214 字节的停用 stdio 服务器配置，磁盘内容一致，既有安装版进程未受影响。下一项任务仍为 `P10-078`。 |
| 2026-08-16 | 完成用户优先插入的 `P10-111` 文件路径猜测与批次读取可靠性修复：`read_file`/`list_directory` 的不存在路径诊断在父目录错误时以最多 2,000 个条目、深度 8 的 BFS 扫描寻找同类型候选，所有候选先 canonicalize 并确认仍在工作区内，符号链接不进入候选；系统提示新增“先发现、等待结果、再读取”的批次约束，避免并行猜测路径。新增 `src/store/workbench.ts` 到 `src/stores/workbenchStore.ts` 的回归覆盖；相关 Rust 单测通过，下一项任务仍为 `P10-078`。 |
| 2026-08-16 | 完成用户优先插入的 `P10-110` Windows PowerShell 原生 `rg` 管道 CRLF 诊断：实际会话中两条 `rg --files ... | rg '...\.js$'` 均为退出码 1 且零输出；使用系统 `powershell.exe` 与内置 ripgrep 15.2.0 确认 PowerShell 5.1 把原生管道文本转为 CRLF，原命令稳定失败，接收端增加 `--crlf` 后稳定返回匹配。execution 层区分 `powershell.exe` 与 `pwsh`，工具恢复提示只命中退出码 1、空输出、真实管道、下游 `rg` 和未转义 `$` 锚点；已有 stderr、PowerShell 7、超时/取消、无锚点和已经使用 `--crlf` 的命令保持原诊断。同步修订 system prompt、`run_command` Schema、架构和 ADR 0014，并把路径通配符示例改为截图中的 `index-*.js`。`pnpm build`、Rust 格式/检查、全量 Rust 测试（291/291）和 `git diff --check` 通过；使用隔离标识 `com.kcoder.validation.p10110`、端口 1423 启动 `pnpm tauri dev`，最新调试窗口响应正常且 `localhost:1423` 返回 200，既有安装版进程未受影响。下一项任务仍为 `P10-078`。 |
| 2026-08-16 | 完成用户优先插入的 `P10-109` 文件工具诊断与 PowerShell `rg` 通配符恢复：实际会话中的两次 `path cannot be resolved` 来自猜测的不存在路径，`path is not a file` 来自把目录传给 `read_file`，`os error 123` 来自 PowerShell 将 `dist/assets/CodeEditor-*.js` 原样交给原生 `rg`。文件工具现在区分预期文件/目录、拒绝通配符、为不存在路径返回最多 3 个同类型候选和明确下一步，系统提示要求先列父目录或搜索内容；PowerShell `rg` 失败会给 Provider 返回 `rg --glob 'CodeEditor-*.js' ... dist/assets` 恢复提示，失败命令行显示有界诊断。内置 `rg.exe` 对照验证原命令退出码 2、修正命令退出码 0；`pnpm build`、Rust 格式/检查、全量 Rust 测试（288/288）、desktop/narrow Playwright（2/2）和 `git diff --check` 通过；`pnpm tauri dev` 已启动，调试进程响应正常且 `localhost:1420` 返回 200。下一项任务仍为 `P10-078`。 |
| 2026-08-16 | 完成用户优先插入的 `P10-108` 长 Turn Token 成本与结构化 Compaction 优化：Provider `contextWindow` 保持硬上限，默认 128,000 窗口另设 96,000 工作基准；请求估算加入运行时指令和工具 Schema，65% 工作高水位触发，压缩后历史与摘要降到低水位；CompactionSummary 升级 v3，保留当前用户请求、近期请求、高信号工具观察和压缩前后估算；工具 Schema 顶层描述限 2 KiB，工具结果限 128 KiB 并保留首尾；Runtime Metrics 记录自动/手动压缩及估算节省。新增上下文回放、Provider 载荷、工具配对、指标聚合与旧摘要兼容测试。`pnpm build`、`cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`、`cargo check --manifest-path src-tauri/Cargo.toml`、单线程全量 Rust 测试（285/285）和 `git diff --check` 通过；使用隔离标识 `com.kcoder.validation.p10108`、端口 1422/9332 启动 `pnpm tauri dev`，真实 Tauri WebView 通过本地假 Provider 累计完成 15 次 `run_turn` 并触发自动压缩；随后手动 `compact_thread` 返回 v3（估算 21,240 -> 1,199 tokens），指标中的 `compactionCount` 从 3 增至 4、估算节省从 170,340 增至 190,381 tokens；验收后删除隔离假 Provider，未发起真实外部模型请求。 |
| 2026-08-15 | 完成用户优先插入的 `P10-107` Windows 内置 ripgrep：按 Codex 的 `codex-path` 思路固定引入官方 ripgrep 15.2.0 Windows x86_64 构建，保存 MIT/Unlicense、发布归档 SHA-256、`rg.exe` SHA-256 与可复现获取脚本；Tauri 将资源映射到 `tools/`，`AppState` 在启动和工作区切换时把宿主验证过的工具目录交给 `CommandRuntime` 与 `NativePtyRuntime`，两者只为子进程生成“内置目录在前”的 `PATH`，不修改全局环境。Windows `run_command` 说明同步声明内置版本。`pnpm build`、Rust 格式检查、`cargo check`、全量 279 项 Rust 测试和 `git diff --check` 通过；使用隔离标识 `com.kcoder.validation.p10107` 与端口 1422/9331 启动 `pnpm tauri dev`，在真实 Tauri WebView 的工作台 PTY 输入 `rg --version`，确认输出 `ripgrep 15.2.0 (rev e89fff89ac)`，截图留存在忽略的 `src-tauri/target/validate-bundled-rg.png`。 |
| 2026-08-15 | 完成用户优先插入的 `P10-106` 命令兼容提示与中文思考摘要平铺：从实际会话 JSONL 确认截图中的 `rg` 搜索失败源于宿主 Windows PowerShell `PATH` 中没有 `rg`，模型随后已用 `Select-String` 完成搜索；Windows `run_command` 契约补充不得假定 `rg` 等 Unix 工具已安装。Provider 原始安全摘要继续完整持久化，系统提示以中英文要求用户可见摘要使用简体中文，界面过滤纯英文及英文叙述占主导的摘要，并把相邻中文摘要合并为无卡片、无折叠状态、无箭头的透明平铺组。`pnpm build`、Rust 格式/检查、全量 276 项 Rust 测试、desktop/narrow Playwright 回归（2/2）和 `git diff --check` 通过；使用隔离标识 `com.kcoder.validation.p10106` 启动 `pnpm tauri dev`，在真实 Tauri WebView 注入运行时事件后确认 1 个摘要组、2 个中文片段、0 个英文摘要及 0 个折叠控件。 |
| 2026-08-15 | 完成用户优先插入的 `P10-105` 模式菜单外部点击关闭：输入区模式选择器打开时以容器边界过滤文档 `mousedown`，点击菜单外的任意 k-Coder 区域会自动关闭，触发器和选项点击不受影响。新增 desktop/narrow Playwright 回归（2/2）；`pnpm build`、Rust 格式/检查、全量 276 项 Rust 测试通过。本次未启动 `pnpm tauri dev`，未声明原生桌面窗口验证。 |
| 2026-08-15 | 完成用户优先插入的 `P10-104` Windows 文件打开修复：工作区内文件先沿用后端规范化与越界拒绝，再为系统 Shell 临时还原 `\\?\` / `\\?\UNC\` 路径；打开文件和资源管理器定位统一改用 `tauri-plugin-opener`，删除 `cmd /C start` 与手工 Explorer 参数，失败改为文件预览内错误反馈。新增 ADR 0036、本地盘符/UNC/越界单测和 desktop/narrow opener 错误回归；`pnpm build`、Rust 格式/检查、全量 276 项 Rust 测试、相关 Playwright 2/2 和 `git diff --check` 均通过。运行中的 `pnpm tauri dev` 已热编译最新代码；在原生桌面窗口打开 `AGENTS.md` 并执行“在资源管理器中定位”后，资源管理器正确打开 `k-coder` 目录，轮询期间没有出现标题或正文含 `\\` 的系统错误框。 |
| 2026-08-15 | 完成用户优先插入的 `P10-103` 无项目会话：输入区左下角项目菜单增加“不在项目中”，选择后创建 `in_project=false` 且无 `workspace_path` 的持久会话，不切换全局工作区；Repository、Turn 工具注册、项目扩展加载、直接子智能体 IPC 与输入候选共同阻断项目能力，旧会话默认按项目模式兼容。新增 ADR 0035、JSONL schema 9、SQLite schema 5、5 项 Rust 安全回归和 desktop/narrow Playwright 回归；`pnpm build`、Rust 格式/检查及全量 276 项 Rust 测试通过，无项目与迁移旧线程兼容 Playwright 4/4 通过。完整 98 项 E2E 运行另有 6 项失败，复跑定位为既有计划浮层旧文案、恢复活动 DOM 和终端重挂载计数三组断言，不经过本任务路径。运行中的 `pnpm tauri dev` 原生 WebView 已实际打开菜单并选择“不在项目中”，截图确认触发器状态，落盘事件为 schema 9、`in_project=false`、0 项工作区绑定。 |
| 2026-08-15 | 完成用户优先插入的 `P10-102` 工具组摘要对齐与箭头收敛：移除摘要左侧终端/扳手 logo，状态点保留；标题后使用独立 15px 右/下箭头并垂直居中，非完成态状态文字放在箭头之后，避免状态宽度改变箭头位置。保留无卡片透明行、活动完成自动收起、命令行状态图标和多命令 168px 组内滚动；新增标题/箭头间距与方向回归。`pnpm build`、Rust 格式检查和 `cargo check` 通过；默认并行 Rust 全量两次均为 268 通过、同一项既有多智能体 `<350ms` 时序断言失败，单项复跑通过，单线程全量 269/269 通过；相关 desktop/narrow Playwright 12/12 通过，原生 `pnpm tauri dev` WebView 进程 PID 9540 响应正常，窗口截图确认真实桌面标题行对齐。综合主工作台中既有计划浮层“已完成”文案断言仍失败 2 项，与本次展示改动无关。 |
| 2026-08-15 | 完成用户优先插入的 `P10-101` 工具组折叠与多命令滚动优化：命令/操作组摘要移除卡片边框、底色和圆角，改为状态点、工具图标、标题及箭头组成的透明轻量行；多命令展开列表限制为 168px 并提供独立纵向滚动，单命令和普通操作组不被强制撑高。修订架构与 ADR 0033/0034；`pnpm build`、Rust 格式/检查、269 项 Rust 测试通过，新增 desktop/narrow Playwright 回归 2/2 通过，相关命令实时/恢复/失败/取消工作流另 8/8 通过；原生 `pnpm tauri dev` WebView 进程响应正常，`localhost:1420` 返回 200，窗口截图确认最新无卡片样式。组合运行中仅有既有主工作台计划浮层“已完成”文案断言 2 项失败，与本次改动无关。 |
| 2026-08-15 | 完成用户优先插入的 `P10-100` 命令活动单行化：实时与历史 `run_command` 统一只显示命令文本和内联状态，移除逐项耗时、Shell/目录/超时参数卡和命令输出折叠；运行时仍保留有界脱敏结果供 Provider、恢复和审计使用。新增 ADR 0034，修订架构与 ADR 0010/0033；`pnpm build`、Rust 格式/检查、desktop/narrow 完成/失败/取消/恢复与完整时间线回归（6/6）和截图检查通过，运行中的 `pnpm tauri dev` WebView 进程响应正常且开发服务已加载最新组件。全量 269 项 Rust 测试两次均有同一项既有并发 `<350ms` 计时断言抖动（268 通过、1 失败），该单测单独复跑通过。 |
| 2026-08-15 | 完成用户优先插入的 `P10-099` Codex 风格工具组折叠：连续命令/操作组的摘要改为状态点、工具图标、动作标题和可选状态的轻量单行，完成态不再重复显示“已完成”，展开内容使用左侧连接线和紧凑工具行；保留既有 `details` 语义、自动展开/收起、参数详情和命令输出。新增 ADR 0033；TypeScript 检查、工具组折叠 Playwright desktop/narrow 回归和视口截图验证通过。综合工作台 E2E 中另有一条与本次样式无关的计划浮层旧断言失败。 |
| 2026-08-15 | 完成用户优先插入的 `P10-098` 对话连续性优化：CompactionSummary 契约升级为 v2，按上限保留最近 4 条用户请求（每条 800 字节），纯图片请求保留数量标记；实时和历史 ContextCompaction Item 增加近期请求计数，旧 v1 摘要按空列表兼容。新增 ADR 0032、上下文与协议回归测试，并通过 TypeScript 检查和定向 Rust 测试。 |
| 2026-08-14 | 完成用户优先插入的 `P10-097` Codex 风格计划进度融合：正式 `update_plan` 计划从 Turn 执行折叠区移到回答后的持续可见胶囊，摘要显示当前步骤，并按当前 Turn 的未撤销变更统计唯一目标文件和累计增删行；顶层浮层支持悬停、键盘聚焦、点击固定、`Escape`/点击外部关闭，完整展示五种中文步骤状态，窄屏和减少动态效果偏好均有明确降级。复用共享 unified diff 行数统计，`todo_write` 任务清单保持独立。`pnpm build`、Rust 格式/检查、`git diff --check` 和全量 266 项 Rust 测试通过；按用户要求未安装或运行 Playwright。检测到仓库外冻结宿主 `D:\apps\k-coder-host\release\k-coder.exe`（PID 732）正在运行，为避免抢占单实例而未启动 `pnpm tauri dev`，本轮不声明真实桌面悬停视觉已复验。 |
| 2026-08-14 | 完成用户优先插入的 `P10-096` 对话图片修复：修正 Rust `ContentBlock::Image` 的 `data_url` 与前端 `dataUrl` 契约不一致，并通过反序列化别名兼容既有 JSONL；Provider 目录升级到内部 schema 2，为 `gpt-4o`、`gpt-4.1`、`gpt-5` 家族的旧默认配置迁移视觉能力，未知模型与 `o3-mini` 保持文本路径，迁移后显式关闭仍会保留；设置中新建模型默认启用图片。`pnpm build`、Rust 格式/检查和全量 266 项测试通过，图片与 Provider desktop/narrow 定向 Playwright 4/4 通过；全量 E2E 为 87 通过、3 跳过，另有与本改动无关且可稳定复现的终端切页计数用例 2 项失败。使用独立 `com.kcoder.image-preview-check` 标识和 `1421` 端口启动原生 Tauri WebView，真实旧 JSONL 图片成功解码为 1556×332 预览；本地假 Provider 捕获到 `gpt-5.6-sol` 的完整 56,938 字符原图且未收到注入的 OCR 文本，冻结宿主 PID 732 未受影响。 |
| 2026-08-10 | 完成用户优先插入的 `P10-095` 项目选择联动：侧栏项目组和输入区选择器改为复用同一派生项目集合，优先使用持久化会话工作区绑定，合并显式添加项目与当前工作区，并仅用后端最近目录补全名称、规范路径和信任信息；历史上打开过但未加入项目的父目录或子目录不再只出现在输入区菜单。侧栏增加与输入区勾选一致的活动态，输入区切换项目创建绑定会话、侧栏选择项目会话后均双向同步；`pnpm build`、项目相关 desktop/narrow Playwright（6 通过、2 条窄屏侧栏用例按设计跳过）、Rust 格式/检查和全量 264 项测试通过。运行中的 `pnpm tauri dev` 原生 WebView 响应正常，`localhost:1420` 返回 HTTP 200，并已截图确认侧栏与输入区项目列表均只有 `k-coder`、`codex`，活动项目一致。 |
| 2026-08-10 | 完成用户优先插入的 `P10-094` 项目路径显示修复：新增共享前端 user-facing 路径转换，项目选择器标题、菜单、搜索与工作台最近项目不再显示 Windows 内部 `\\?\` 前缀，并正确把 `\\?\UNC\server\share` 还原为 `\\server\share`；路径比较和去重共用同一转换，但 `ProjectRecord`、会话绑定及 `switch_workspace` 继续使用原始规范路径。`pnpm build`、desktop/narrow 定向 Playwright 2/2、Rust 格式/检查和全量 264 项测试通过；运行中的 `pnpm tauri dev` 原生 WebView 已重新加载，并截图确认真实项目注册表中的本地路径不再泄漏前缀。 |
| 2026-08-10 | 完成用户优先插入的 `P10-093` 活动会话恢复展示修复：显式区分实时启动与从历史/mailbox 恢复的活动 Turn，切回会话时立即完整展示已持久化正文，不再从头重放造成“任务重新开始”的错觉；恢复后的新正文仍保持实时展示节奏，切换过程不调用 `turn_start`/`turn_retry`。新增 desktop/narrow 切换、即时恢复、后续增量与命令零调用回归，相关 E2E 7 项通过、1 项按窄屏设计跳过；`pnpm build`、Rust 格式/检查和全量 264 项测试通过。`pnpm tauri dev` 原生窗口响应正常，`localhost:1420` 返回 HTTP 200，并已截图确认实际 WebView 渲染。 |
| 2026-08-10 | 完成用户优先插入的 `P10-092` 对话项目选择：输入区持续展示当前项目，菜单读取后端最近项目并支持名称/路径搜索和本地新建；选择其他项目时先通过既有工作区切换门禁同步后端，再创建目标项目绑定的新会话，不改写已有会话绑定。新增 desktop/narrow 选择、命令顺序和横向边界回归；`pnpm build`、定向 Playwright 2/2、Rust 格式/检查和全量 264 项测试通过。`pnpm tauri dev` 原生窗口响应正常，`localhost:1420` 返回 HTTP 200，并已截图确认实际 WebView 显示当前项目入口。 |
| 2026-08-09 | 完成用户优先插入的 `P10-091` Gemini 图像生成修复：识别 `gemini-3-pro-image-preview` 等图像模型，改用 `generateContent` 单次响应并请求 `TEXT/IMAGE` 模态，避免固定调用 `streamGenerateContent`、工具声明和推理字段造成连接失败；解析有界 `inlineData` 为助手 `Image` 内容块，支持 JSONL 历史恢复和会话图片预览。新增 Gemini URL/响应、Agent 图片持久化与非法图片拒绝回归；`pnpm build`、Rust 格式/检查和全量 Rust 264 项测试通过。 |
| 2026-08-09 | 完成用户优先插入的 `P10-090` 普通操作组图标调整：完成态“执行了操作/执行了多个操作”由活动波形改为深色扳手图标，命令组继续使用深色终端图标，两者统一取主题 `--color-ink`；运行、失败和取消状态反馈不变。同步修订架构与 ADR 0010，新增 desktop/narrow 图标及主题色断言，定向 E2E 2/2 通过；`pnpm build`、Rust 格式/检查和单线程全量 260 项 Rust 测试通过。默认并行 Rust 全量中的既有多智能体 350ms 时序断言两次受套件争用超时，单项 0.29s 与单线程全量复核通过。使用独立应用标识和端口启动 `pnpm tauri dev`，原生窗口响应正常、`127.0.0.1:1421` 返回 HTTP 200，并已截图确认 WebView 正常渲染。 |
| 2026-08-09 | 完成用户优先插入的 `P10-089` 对话摘要图标调整：“思考摘要”由脑图标改为灯泡图标，完成态“执行了 …”由活动波形改为勾选圆图标，运行中、失败和取消状态反馈不变；新增 desktop/narrow 直接图标断言，定向 E2E 4/4 通过。`pnpm build`、Rust 格式/检查和全量 260 项 Rust 测试通过；Rust 首次全量运行中的既有多智能体 350ms 时序断言受并行编译负载影响失败，单项复核和无并行负载的完整重跑通过。使用独立应用标识启动 `pnpm tauri dev` 调试实例，原生窗口响应正常，`localhost:1420` 返回 HTTP 200，并已实际截图确认应用渲染。 |
| 2026-08-09 | 完成用户优先插入的 `P10-088` 思考摘要可见性降噪：reasoning 事件继续完整持久化和恢复，但短小且只描述规划、准备、检查、运行、测试等动作的过程句不再渲染原文，活动 Turn 只保留紧凑状态；包含结论、原因、风险、约束或足够上下文的安全内容改称“思考摘要”并按既有规则折叠。同步修订架构、ADR 0009/0010，以及失败 Turn 中仅有过程短句时不生成空思考行的回归。`pnpm build`、Rust 格式/检查和 259 项 Rust 测试通过；相关 desktop/narrow E2E 4/4 通过，完整 Playwright 为 81 通过、3 跳过，仅余 2 个既有终端标签重复启动断言失败（期望 2 次，当前 4 次）。重新启动 `pnpm tauri dev` 后原生 `k-Coder` 窗口响应正常，`localhost:1420` 返回 HTTP 200，并已实际截图确认应用渲染。 |
| 2026-08-09 | 完成用户优先插入的 `P10-087` 对话审计信息降噪：模型调用用量、Provider 上下文、操作确认结果和自动批准事件保留在事实时间线与历史恢复中，但不占据对话过程；待处理手动审批仍显示可操作卡片，设置页继续提供用量追踪，输入区只让完整访问保持醒目文字状态。同步移除标题区重复 Token 数、清理只有隐藏审计事件的空助手行，并修正终态旧思考仍显示“生成中”的误导状态；修订架构与 ADR 0010，新增 desktop/narrow 回归。`pnpm build`、Rust 格式/检查和 259 项 Rust 测试通过；完整 Playwright 为 81 通过、3 跳过，仅余 2 个既有终端标签重复启动断言失败（期望 2 次，当前 4 次）。运行中的 `pnpm tauri dev` 原生窗口响应正常，`localhost:1420` 返回 HTTP 200，并已实际截图确认标题、时间线和权限状态。 |
| 2026-08-09 | 完成用户优先插入的 `P10-086` Codex 式多层折叠：所有完成、失败和取消 Turn 统一折叠成单行摘要；展开外层后，失败原因与重试保持直接可用，计划、思考、工具组、生命周期步骤和命令输出各自独立折叠，仅运行中的过程自动展开，工具结束时立即收起。同步修订架构与 ADR 0010，并覆盖实时切换、恢复、完成、失败、取消、审批和 Monaco 懒加载的 desktop/narrow 回归。`pnpm build`、Rust 格式/检查和 259 项 Rust 测试通过；完整 Playwright 为 81 通过、3 跳过，仅余 2 个既有终端标签会话保持断言失败（期望启动 2 次，当前为 4 次）。运行中的 `pnpm tauri dev` 桌面窗口响应正常，开发服务 `127.0.0.1:1421` 返回 HTTP 200。 |
| 2026-08-09 | 完成用户优先插入的 `P10-085` Agent 对话失败态改造：外层 Turn 收敛为单一“请求未完成”摘要，状态栏紧凑展示操作数与耗时；终态事件仍保留在事实时间线，但不再作为内部同义步骤重复渲染，展开后直接显示错误原因和最后一次失败/停止 Turn 的就地重试。移除 Turn 失败触发的全局错误条以及已有终态时间线消息尾部的“生成失败/已停止”，旧无时间线历史继续兼容。深色 desktop/narrow 截图确认长错误文本与按钮无重叠，相关定向 E2E 8/8 通过；`pnpm build`、Rust 格式/检查和全量 259 项 Rust 测试通过。完整 Playwright 为 81 通过、3 跳过，仅余 2 个既有终端标签会话保持断言失败（期望启动 2 次，当前为 4 次）。独立 `pnpm tauri dev` 调试窗口响应正常，开发服务 `127.0.0.1:1421` 返回 HTTP 200。 |
| 2026-08-09 | 完成用户优先插入的 `P10-084` 会话恢复重复回复修复：展示层在统一历史与实时投影交汇处恢复助手 Item 的稳定 Turn 归属，并按 Turn 合并重复投影、优先保留完成态和最终正文，避免同一持久化回复同时作为消息与孤立 Turn 再渲染。新增缺失 `turnId` 的恢复场景与同 Turn 多投影场景，desktop/narrow 定向 E2E 4/4 通过；`pnpm build`、Rust 格式/检查和全量 259 项 Rust 测试通过。完整 Playwright 为 79 通过、3 跳过，另有 2 个与本次会话投影无关的既有终端标签会话保持断言失败（期望启动 2 次，当前为 4 次）。独立 `pnpm tauri dev` 调试二进制和 `k-Coder` 主窗口已启动且响应正常，开发服务 `127.0.0.1:1421` 返回 HTTP 200。 |
| 2026-08-09 | 完成用户优先插入的 `P10-083` 普通 Turn 资源治理：无活动 Goal 的根 Turn 在 `30` 次 Provider 请求、`1,000,000` 累计 tokens 或 `10` 分钟后通过类型化 `turn_continuation` UserInput Item 请求继续、压缩后继续或停止；续跑重置分段额度，不改变显式预算硬边界和无固定生命周期上限。自动 Compaction 提前到 65%，最近工具结果缩为 2 个，超过 4 KiB 的结果使用首尾确定性摘要。新增 ADR 0031、运行时三分支、Goal 例外、旧协议兼容、压缩策略和双视口卡片回归；`pnpm build`、Rust 格式/检查、全量 259 项 Rust 测试及 desktop/narrow 定向 E2E（2/2）通过。`pnpm tauri dev` 调试二进制与 `k-Coder` 主窗口已启动，开发服务返回 HTTP 200；本次未触发真实模型请求。 |
| 2026-08-09 | 修复 `P10-082` 主机 Shell 工作目录的 `\\?\` 前缀泄漏：PTY `start_pty` 和命令执行路径在 `resolve_cwd` 之后新增 `strip_verbatim_prefix`，把 `canonicalize` 注入的 `\\?\` / `\\?\UNC\` 前缀还原成 user-facing 形式再交给 `CommandBuilder` / `tokio::process::Command`，与 `extensions/user_facing_path` 对诊断文案的处理保持一致；同步补一条覆盖本地与 UNC 两种形式的单测。`cargo fmt --check`、`cargo check`、Rust 全量 252 项测试通过；前端对话/工作台重渲染治理的变更见上一条。 |
| 2026-08-09 | 完成 `P10-077` 随批模块拆分：在既有 thread operation gate、按线程 writer、增量 history index 和 storage 历史分页边界上，继续提取 `agent/input.rs`、`agent/provider_history.rs`、`commands/threads.rs` 与前端 `historyProjection.ts`；公共协议、Tauri invoke 名称和 JSONL 事实保持不变，并补充输入持久化、Provider 历史修复、命令错误映射及前端历史投影回归。`pnpm build`、前端 138 项测试、Rust 格式/检查、全量 Rust 251 项测试和完整 Playwright（75 通过、3 条既有条件跳过）通过；Vite 依赖缓存强制重建后，`pnpm tauri dev` 调试二进制启动，`localhost:1420` 返回 HTTP 200，`k-Coder` 主窗口响应正常，本次未触发真实模型请求。 |
| 2026-08-09 | 对话与工作台重渲染治理（与 `P10-077` 同期）：
- `MarkdownContent` 与 `MarkdownDocument` 改用 `React.memo`，流式回复只在新文本到来时才重解析；
- `WorkbenchPanel` 的 `DirectoryNode`/`FolderNode`/`FileTypeIcon` 递归组件 `memo` 化，`select`/`save`/`closePreview`/`attach` 用 `useCallback` 稳定引用，keydown `useEffect` 加上正确依赖数组；
- `TerminalPanel` 的 `display:none` 隐藏策略改为条件渲染，切到别的 Tab 时真正卸载 xterm/PTY/ResizeObserver，避免隐藏态持续 90ms 轮询；
- `App.tsx` 的派生数据（`toolActivities`、`activitiesByTurn`、`timelineByTurn`、`representedTurnIds`、`assistantMessagesByTurn`、`orderedTurnIds`、`retryTurnGroupsByUserMessage`、`groupedRetryTurnIds`、`planTurnId`、`orphanTurnIds` 等）用 `useMemo` 缓存；`ConversationTurnActivity` 同步 `memo`。`pnpm build`、Rust 格式/检查/全量测试通过；未启动 `pnpm tauri dev` 实际重放工作流，桌面端的实际帧率收益需在 `pnpm tauri dev` 下复测。 |
| 2026-08-08 | 完成用户优先插入的 `P10-082` 工作台交互终端：工作台新增"终端"标签，使用本地打包的 `@xterm/xterm` 渲染既有宿主 PTY 会话；`start_pty` 空 `program` 改为启动平台默认交互式 Shell（复用 `execution/shell.rs` 检测逻辑，PowerShell/CMD 附带 UTF-8 初始化），`cwd` 为空时固定为当前工作区根目录；标签切换保持会话，退出后显示终态并可重启，轮询、写入、缩放和关闭均走类型化 IPC。新增 Rust 默认 Shell PTY 回归、`interactive_launch` 参数契约测试和双视口终端 E2E；`pnpm build`、Rust 格式/检查、execution 模块 16 项测试和双视口终端 E2E（4/4）通过。 |
| 2026-08-08 | 更新 `docs/Codex对话优化计划.md`：同步 `P10-071` 队列消息原子 steer 的范围与验收结果，并把对照 Codex 后确认的剩余工作拆为 Thread 生命周期操作门、按线程有界 writer、SQLite 增量历史索引、事件驱动 mailbox 同步、结构化错误、随批模块拆分和 Monaco 包体治理七个候选批次。每批补充问题证据、实施边界、依赖和验收出口；候选项尚未进入开发，当前下一任务仍为 `P10-001` Phase 10 生产加固验收。 |
| 2026-08-08 | 完成用户追加的 `P10-071` 队列消息原子 steer：新增只携带 `threadId`、`expectedTurnId` 和 `queuedTurnId` 的 `turn_steer_queued`，正文与附件始终从 Rust mailbox 读取；`AppState` 在精确活动 Turn 锁内调用 mailbox 原子接纳，只有 `TurnControl` 接受输入后才删除 pending 项，worker 抢先、Turn 已关闭、重复请求和 retry 项均保留确定性失败语义。工作台不再组合 `turn_steer` 与 `remove_queued_turn`，新增公共载荷序列化、mailbox 接纳/关闭、活动 Turn/队列匹配和双视口单命令 E2E 回归。`pnpm build`、Rust 格式/检查、全量 Rust 240 项测试和完整 E2E（70 通过、2 条窄屏条件跳过）通过；现有 `pnpm tauri dev` watcher 已编译并启动最新调试二进制，开发服务返回 HTTP 200，`k-Coder` 主窗口响应正常，本次未触发真实模型请求。 |
| 2026-08-08 | 完成用户优先插入的 `P10-081` 命令对话展示调整：工具项直接显示命令文本、实际 Shell、工作目录和超时；Shell 优先读取结果元数据，缺失时显示通用 `Shell`，避免把 PowerShell 误标为 CMD。同步修正桌面时间线回归断言。`pnpm build`、Rust 格式/检查和全量 Rust 测试通过；定向 E2E 的命令输出断言通过，后续 Monaco Diff 断言受 Vite 依赖优化错误影响未完成。 |
| 2026-08-08 | 推进 `P10-077` 模块拆分：将 `storage/mod.rs` 中的历史 turns/items 游标分页、游标编解码和 Item 视图裁剪提取至 `storage/history_pagination.rs`，保留原有页大小、游标绑定、陈旧游标拒绝和分页结果语义；Rust 全量 249 项测试、格式检查、类型检查、前端生产构建和 `git diff --check` 通过。agent、commands 与 frontend reducer 的剩余职责拆分仍待继续，`P10-078` 暂不提前开发。 |
| 2026-08-08 | 对照 Codex 的历史回放语义补强对话持久化：`rollback_thread` 先应用已有回退标记再计算当前有效终态，连续 rollback 可继续工作；`fork_thread` 只复制有效历史，避免恢复已回退 Turn。新增存储回归覆盖连续 rollback、重启恢复和 rollback 后默认 fork；目标仍属于 `P10-001` 生产加固，不新增协议版本。 |
| 2026-08-08 | 完成用户追加的 `P10-070` 异步重试与精确恢复：新增 `turn_retry` 和带类型 mailbox 工作项，重试复用原消息、模式、控制器及主 AgentRuntime，并让 handle、实时事件和历史共享预分配 `turnId`；`QueuedTurn.kind` 区分普通消息/重试且兼容旧载荷。桌面重试不再等待阻塞 `retry_turn`，停止与错误恢复只调用精确 `turn_interrupt` 并重新同步运行时状态，不再调用线程级 `cancel_turn`；队列文案改为"加入当前对话"，重试项仍可删除。新增 ADR 0025、Rust mailbox/重试身份回归和双视口异步重试/精确恢复 E2E。`pnpm build`、Rust 格式/检查、单线程全量 Rust 237 项测试和完整 E2E（70 通过、2 条窄屏条件跳过）通过；默认并行 Rust 全量中的既有多智能体 350ms 时序断言两次受负载影响超时，单项 0.29s 复核通过。现有 `pnpm tauri dev` watcher 已加载最新调试二进制，开发服务返回 HTTP 200，`k-Coder` 主窗口响应正常。 |
| 2026-08-08 | 完成用户指定的 `P10-067` 至 `P10-069`：Rust `ThreadMailbox` 统一拥有每线程单活动 Turn 和 FIFO 待处理输入，桌面队列只投影后端 snapshot；新增带 `expectedTurnId` 的 `turn_steer` 与 `turn_interrupt`，steer 在原 Turn 的下一次 Provider 边界继续且不取消，过期/无活动控制请求拒绝；新增 `thread_fork`、`thread_resume`、`thread_rollback`，fork 只复制选定的已完成对话历史且不复制可撤销工作区变更，rollback 追加审计标记并只截断有效对话历史、不改动本地文件。事件 schema 升至 v4，JSONL schema 升至 8；阻塞 `run_turn` 仅保留迁移兼容。新增公共载荷、FIFO、控制竞态、steer 续接、fork/rollback 重启恢复和双视口 mailbox E2E；`pnpm build`、Rust 格式/检查、全量 Rust 236 项测试和完整 E2E（68 通过、2 条窄屏条件跳过）通过。Rust 首次最终重跑中的既有多智能体 350ms 时序断言偶发超时，单项复核和完整重跑通过；现有 `pnpm tauri dev` watcher 已重编译最新调试二进制，开发服务返回 HTTP 200，`k-Coder` 主窗口响应正常。 |
| 2026-08-08 | 完成用户优先插入的 `P10-066` 异步 `turn_start` 与 TurnHandle：`protocol/` 新增版本化 handle，Tauri 边界预分配稳定 `turnId`，AgentRuntime 以该 ID 写入并发布完整生命周期；启动命令在 `turn_started` 已持久化并发布后返回，Provider/工具循环留在后台，终态只通过公共事件与统一历史查询获得。工作台正常发送不再等待阻塞 `run_turn`，并防止极短 Turn 的迟到 handle 覆盖已到达终态；旧阻塞入口继续复用同一执行函数。新增 handle 序列化、启动握手、启动错误、固定 ID 事件/JSONL/查询回归及双视口异步调用断言；`pnpm build`、Rust 格式/检查、全量 Rust 228 项测试和完整 E2E（68 通过、2 条窄屏条件跳过）通过，最新 `pnpm tauri dev` 调试二进制已热重载，开发服务返回 HTTP 200，`k-Coder` 主窗口响应正常。 |
| 2026-08-08 | 完成用户优先插入的 `P10-065` 统一 Thread Item 历史投影与分页读取：在 `protocol/` 定义版本化 Turn、Item 和分页载荷，`storage/` 从 JSONL 事实只读构建消息、Reasoning、Tool、Approval、UserInput、Change、ContextCompaction 及时间线片段；新增 `read_thread_history`、`list_thread_turns`、`list_thread_items`，支持 1-100 页大小、正反排序、Item 视图、Turn 过滤及绑定查询条件的不透明游标。工作台正常路径不再从多套快照猜测时间线，并可加载更早 Turn；旧事件缺少 `TurnStarted` 或生命周期时仍能恢复且不反写伪造事实。新增 Rust 公共契约/旧事件/分页拒绝回归和双视口统一历史 E2E；`pnpm build`、Rust 格式/检查、全量 Rust 224 项测试和完整 E2E（68 通过、2 条窄屏条件跳过）通过，最新 `pnpm tauri dev` 调试二进制已运行，开发服务返回 HTTP 200，`k-Coder` 主窗口响应正常。全量 Rust 首次运行中的既有多智能体 350ms 时序断言偶发超时，单独复核和完整重跑通过。 |
| 2026-08-07 | 完成用户优先插入的 `P10-064` 交互类 Item 生命周期：Approval、UserInput、Change 和 ContextCompaction 分别复用请求 ID、变更 ID 与压缩事实事件 ID；请求/解决、审计变更和自动压缩实时事件按同一身份严格排序，成功、失败、取消均确定性闭合。变更审计失败会在回滚后关闭失败 Item，Turn 收尾与崩溃恢复会关闭仍活动的 Item，缺少启动事实的旧会话不伪造完成事件。新增成功、拒绝/超时/跳过、审计回滚、手动/自动压缩、崩溃取消和旧事件兼容回归；`pnpm build`、Rust 格式/检查、全量 Rust 222 项测试和完整 E2E（66 通过、2 条窄屏条件跳过）通过。最新 `pnpm tauri dev` 调试二进制已运行，开发服务返回 HTTP 200，`k-Coder` 主窗口响应正常。 |
| 2026-08-07 | 完成用户优先插入的 `P10-063` Tool Item 生命周期：每个已接受 Tool call 使用现有 `call.id` 写入/发布 `item_started(tool)`，ToolResult 与 `tool_completed` 后写入/发布成功、失败或取消闭合；取消批次中的待执行调用也会闭合，恢复投影按 Item 状态修正工具活动。新增成功、失败、取消实时/JSONL 回归和前端兼容事件；`pnpm build`、Rust 格式/检查、单线程全量 Rust 220 项测试和完整 E2E（66 通过、2 条窄屏条件跳过）通过。并行 Rust 全量命令中的既有多智能体 350ms 时序断言偶发超时，单独重跑通过。 |
| 2026-08-07 | 完成用户优先插入的 `P10-062` Reasoning Item 生命周期：安全摘要首个增量或直接完成时写入/发布 `item_started(reasoning)`，完成摘要后写入/发布成功闭合，重复完成事件去重；摘要超限、Turn 失败和取消会闭合未完成 Reasoning Item，摘要事实仍不进入 Provider 历史。新增正常/直接完成、超限失败、流中取消和前端生命周期兼容回归；`pnpm build`、Rust 格式/检查、相关 Rust 测试通过。 |
| 2026-08-07 | 完成用户优先插入的 `P10-061` Codex 式助手 Item 生命周期第二阶段：事件 schema 升至 v3，新增 `item_started`/`item_completed` 及 `agent_message` 类型；运行时把助手 Item 的启动、正文增量、最终消息和成功/失败/取消闭合统一到同一 ID，JSONL 事实事件 schema 升至 7，旧事件仍可升级读取；前端从 `item_started` 接管流式助手消息身份。新增协议、运行时持久化和双视口流式生命周期回归；`pnpm build`、Rust 217 项测试、相关 E2E（2/2）通过。 |
| 2026-08-07 | 完成用户优先插入的 `P10-060` Codex 式对话协议演进第一阶段：新增 ADR 0015，把 `Thread -> Turn -> Item` 定为分阶段迁移方向；实时事件 schema 升至 v2，助手正文增量携带运行时生成的稳定 `itemId`，最终消息和工具前说明持久化记录复用同一身份；存储 schema 升至 v6，并为旧事件保留基于事实事件 ID 的确定性投影；前端优先按 `turnId + itemId` 精确归并，仅对 v1 事件保留兼容回退。`pnpm build`、Rust 格式/检查、216 个 Rust 测试、完整双视口 E2E（66 通过、2 条窄屏条件跳过）和 `git diff --check` 通过。运行中的 `pnpm tauri dev` 已热重载最新桌面二进制，开发服务返回 HTTP 200，`k-Coder` 主窗口响应正常；本阶段未触发真实模型请求，通用 Item 生命周期和真正的 steer/interrupt 留待后续阶段。 |
| 2026-08-07 | 完成用户优先插入的 `P10-059` 文件变更编辑器展示：`apply_patch` 和 `write_file` 参数详情改为展开时按需加载的只读 Monaco；`change_applied` 逐文件使用持久化前后快照渲染按扩展名高亮的内联 Monaco Diff，显示增删行统计并支持复制 Diff。新增、删除、移动沿用同一契约，快照因大小边界缺失时降级到已持久化 unified diff，不重新读取当前磁盘。新增桌面/窄屏快照 Diff、统一 Diff 降级和补丁编辑器回归；`pnpm build`、Rust 格式/检查、215 个 Rust 测试、完整双视口 E2E（66 通过、2 条窄屏条件跳过）和 `git diff --check` 通过。运行中的 `pnpm tauri dev` 已加载最新前端且主窗口响应正常，精确文件变更交互由桌面/窄屏 E2E 和截图验证。 |
| 2026-08-07 | 完成用户优先插入的 `P10-058` Shell 命令编辑器展示与停止终态保持展开：`run_command` 参数详情改为用户展开时按需加载只读 Monaco，按实际 PowerShell、CMD 或 Unix Shell 显示标签、行号和语法高亮，并保留工作目录与超时；命令审批摘要适配当前 `command` 文本契约。活动、失败或取消 Turn 的思考、计划、工具与详情步骤保持展开，纯失败/取消终态仍可见，只有 `turn_completed` 成功终态自动收缩。`pnpm build`、Rust 格式/检查、215 个 Rust 测试、完整双视口 E2E（66 通过、2 条窄屏条件跳过）和 `git diff --check` 通过；运行中的 `pnpm tauri dev` 已重建最新桌面二进制，真实 Tauri WebView 主窗口响应正常，命令编辑器与停止终态精确交互由桌面/窄屏 E2E 覆盖。 |
| 2026-08-07 | 完成用户优先插入的 `P10-057` 用户消息复制：用户发送的对话气泡在角色行右侧新增复制按钮，仅当消息存在文本时显示；点击把原始消息文本写入剪贴板并显示 1.5 秒勾选反馈，失败时静默恢复。纯前端展示改动，不触碰 Provider、持久化、队列或时间线。新增桌面/窄屏剪贴板回归；`pnpm build`、相关双视口 E2E（2/2）和 `git diff --check` 通过。 |
| 2026-08-07 | 完成用户优先插入的 `P10-056` 会话工作区强绑定：新增 `thread_workspace_bound` JSONL 事实事件、`sessions.workspace_path` SQLite 投影与前端 `ThreadSummary.workspacePath`；新会话创建即绑定当前规范工作区，旧会话首次执行惰性绑定且冲突绑定关闭失败，并串行化并发首次绑定。项目会话创建、选择、启动恢复和发送前查询后端真实工作区并先切换；普通 Turn、重试与 Compaction 在任何模型或工具工作前校验绑定，工作区切换与 Turn 启动通过活动表复核避免竞态。新增存储不可变绑定、并发首次绑定、旧会话迁移、错工作区拒绝、数据库迁移和项目新建会话切换顺序回归。`pnpm build`、Rust 格式/检查、215 个 Rust 测试、相关 E2E（2/2）和 `git diff --check` 通过；完整双视口 E2E 为 62 通过、2 跳过、2 条既有命令审批摘要断言失败。运行中的 `pnpm tauri dev` 已重建并加载最新桌面二进制，实际在 `k-coder` 项目中新建空会话后确认持久化事件绑定到该项目的规范路径。 |
| 2026-08-07 | 完成用户优先插入的 `P10-055` 内置 Skill：Tauri resources 随包携带 `workspace-review/SKILL.md`，桌面启动把只读资源根注入 `ExtensionService`，发现与热刷新指纹按 `builtin < global < project` 处理同名覆盖，设置页将来源本地化为"内置"，资源根缺失时明确关闭失败；新增真实资源契约、三层优先级、资源根缺失/逃逸和修订指纹回归。`pnpm build`、Rust 格式/检查、212 个 Rust 测试、相关双视口 E2E（4/4）、调试 NSIS 安装包构建和 `git diff --check` 通过；全新 `pnpm tauri dev` 实例已实际打开 Skills 设置，确认内置 Skill 从打包资源目录加载并显示为启用。 |
| 2026-08-07 | 完成用户优先插入的 `P10-054` 流式时间线严格保序：展示投影以最早一段尚未揭示完成的正文作为可见前缀边界，只推进该段文本，后到的工具、事件和后续正文在追平后才按事实顺序依次显示；工具状态、持久化、终态和队列仍即时处理，等待期间活动标题保持"生成回复中"。新增"工具前说明 -> 已完成命令 -> 最终回复 -> Turn 完成"的桌面/窄屏回归；`pnpm build`、Rust 格式/检查、208 个 Rust 测试、相关双视口 E2E（6/6）和 `git diff --check` 通过。`pnpm tauri dev` 已拉起最新桌面二进制，主窗口响应正常且开发服务返回 HTTP 200；既有 Vite 占用 1420 端口使重复的 `beforeDevCommand` 报端口占用，但未阻止桌面进程使用该服务启动。 |
| 2026-08-08 | 完成用户优先插入的 `P10-079` 设置弹窗视图保持：打开设置时不再强制切换到“会话”tab，从“项目”tab 进入并关闭设置后继续显示项目列表；新增桌面项目视图回归断言。 |
| 2026-08-08 | 完成用户优先插入的 `P10-080` 线程切换视图清理：选择新线程后，在异步历史、计划、目标和 mailbox 恢复完成前立即清空上一线程的展示状态；新增双线程延迟读取回归，验证旧消息和计划不会在加载窗口残留。 |
| 2026-08-06 | 完成用户优先插入的 `P10-053` 命令/自动操作收缩体验：终态切换时复用同一执行过程、工具组、思考和生命周期折叠节点，不再卸载后立即重建；使用 `details::details-content` 的 380ms 高度缓出、240ms 透明度过渡和 320ms 箭头过渡，保留旧 WebView 的内容面板降级，并尊重 `prefers-reduced-motion`。新增桌面/窄屏中间高度回归；`pnpm build`、相关时间线 E2E（16/16）和 `git diff --check` 通过。 |
| 2026-08-06 | 完成用户优先插入的 `P10-052` 输入唤醒与深色卡片可读性：新增有界工作区文件名搜索 IPC，`@` 候选插入工作区相对路径；扩展候选通过 `getExtensionOverview` 提供已发现 Skill，选择后插入 `/skill-name`，运行时显式识别该调用且继续要求 Skill 已启用；文件搜索跳过忽略目录和符号链接，并在返回前重新规范化目标路径。提问卡片统一使用 CodeBuddy 深/浅主题语义令牌。新增文件搜索、符号链接边界、Skill 启用状态、输入候选和深色卡片对比度回归；`pnpm build`、Rust 格式/检查、208 个 Rust 测试和 @/ 交互 E2E 通过。 |
| 2026-08-06 | 完成用户优先插入的 `P10-051` 文件预览弹窗居中：移除工作台打开态创建固定定位包含块的空 `transform`，预览遮罩改为固定覆盖窗口内容区，弹窗使用整个视口的可用宽高居中，标题栏保持可见，桌面和窄屏沿用同一套响应式尺寸约束；更新工作台 E2E 几何断言并检查双视口截图，验证弹窗不再锚定右侧列。`pnpm build`、Rust 格式/检查、单线程 207 个 Rust 测试、居中预览相关桌面/窄屏 E2E（2/2）和 `git diff --check` 均通过；默认并行 Rust 全量测试两次仅有既有的多智能体 350ms 计时断言抖动，该用例单独复跑及单线程全量复跑均通过。 |
| 2026-08-06 | 完成用户优先插入的 `P10-050` 对话流式展示节奏与跟随滚动：`turnTimeline` 和运行时终态继续立即进入前端事实状态，活动 Turn 的助手正文在组件展示层以 32ms 节拍按积压量自适应揭示并显示输入光标；完成、失败或取消到达后，展示层先排空已接收正文，再显示终态并折叠执行过程，不阻塞 Provider、工具、持久化或消息队列。消息区改用内容尺寸观察跟随最新内容，只在视口贴近底部时自动下移，明确的向上滚轮、触摸和滚动条拖动会暂停跟随，回到底部后恢复；动画帧执行前二次检查用户意图，避免窄屏竞态把视口拉回底部。新增终态排空和滚动暂停/恢复的桌面、窄屏回归；`pnpm build`、Rust 格式/检查、207 个 Rust 测试、相关桌面/窄屏 E2E（4/4）和 `git diff --check` 均通过。完整 E2E 为 55 通过、1 个条件跳过、4 个失败，失败来自本次范围外的工作台 Markdown 预览默认态和既有命令审批摘要断言。现有 `pnpm tauri dev` 进程来自本仓库，桌面主窗口可响应，开发服务返回 HTTP 200。 |
| 2026-08-05 | 完成用户优先插入的 `P10-049` 平台默认 Shell 命令契约：参考 Codex 将模型可见的 `run_command` 从 `program + args` 改为 `command` 文本；Windows 按 `pwsh -> Windows PowerShell -> cmd.exe` 选择 shell，PowerShell 使用 `-NoProfile -Command` 和 UTF-8 输出前缀，Unix 使用受支持的用户默认 shell 并回退到 `zsh/bash/sh`；宿主结构化进程 API、工作区规范化、超时、取消、进程树清理、有界脱敏输出保持不变，组合或动态脚本保守进入审批，旧历史参数继续只读展示。新增 shell 转换、Schema、策略、流式输出与取消测试；`pnpm build`、Rust 格式/检查、207 个 Rust 测试、命令详情相关桌面/窄屏 E2E（4/4）和 `git diff --check` 均通过。现有 `pnpm tauri dev` 监听器已重建最新二进制，桌面进程和 WebView 可响应，开发服务返回 HTTP 200。 |
| 2026-08-05 | 完成用户优先插入的 `P10-048` 交错进度说明与实时上下文压缩：公共运行时指令要求首次工具前及探索、修改、验证工具批次之间报告动作、已确认事实和下一步，同时禁止私有思维链；80% 自动 Compaction 阈值和确定性压缩保持不变，新增落盘后发布的 `context_compacted` 公共事件，使当前 Turn 无需刷新即可显示默认收起的压缩步骤，事件仅发送稳定 ID、来源和三项计数，不复制内部摘要或工具结果正文；纯 `run_command` 组继续使用"命令"，读取、搜索、编辑等工具改用"操作"，并记录当时的 `program + args` 直接执行边界，该边界随后由 `P10-049` 取代。新增系统提示、公共事件序列化、自动压缩发布和双视口时间线回归；`pnpm build`、Rust 格式/检查、201 个 Rust 测试、完整桌面/窄屏 E2E（55 通过、1 个条件跳过）、收紧载荷后的相关双视口 E2E（2/2）和 `git diff --check` 均通过。标准 `pnpm tauri dev` 文件监听器已重建最新二进制，桌面主窗口可响应，开发服务返回 HTTP 200。 |
| 2026-08-05 | 完成用户优先插入的 `P10-047` 输入字号与桌面单实例约束：对话输入区正文和占位文字由浏览器默认 16px 收紧为 13px；Tauri 单实例插件在插件链首位注册，重复启动时后启动进程退出，首个进程恢复、取消最小化并聚焦主窗口，同时显示"k-Coder 已在运行"原生提示。新增双视口字号回归和提示文案契约测试；`pnpm build`、Rust 格式/检查、199 个 Rust 测试、相关桌面/窄屏 E2E（2/2）和 `git diff --check` 均通过。`pnpm tauri dev` 桌面实例已实际验证，二次启动后进程数保持为 1，并捕获到标题正确的原生提示框。 |
| 2026-08-05 | 完成用户优先插入的 `P10-046` 对话活动摘要文案统一：单个纯 `run_command` 组保留"运行了命令"，其他工具组统一显示"运行了多个命令"；实时和恢复的文件变更步骤统一显示"编辑了文件"，修改类文件明细显示"已编辑 文件名"。同步更新前端 E2E、Rust 恢复契约、架构和 ADR 0010；`pnpm build`、Rust 格式/检查、198 个 Rust 测试、相关桌面/窄屏 E2E（6/6）和 `git diff --check` 均通过，`pnpm tauri dev` 已启动且桌面主窗口可响应。 |
| 2026-08-05 | 调整 `P6-006` 对话图片输入：活动模型勾选"支持图片"时跳过本地 OCR 并把原图发送给 Provider，图片 Turn 的回退目标只保留多模态模型；未勾选或能力未知时由后端运行 PP-OCRv5，只把有界识别 `Context` 投影给 Provider，原图仅保留在领域消息和界面中，识别为空或失败时明确终止。前端移除粘贴即 OCR 和发送等待，对话图片改为真实缩略图按钮，新增支持 Esc、背景/按钮关闭、焦点恢复和响应式 `contain` 展示的全屏预览。新增能力路由、非多模态原图剥离、回退过滤及桌面/窄屏粘贴、发送、预览、关闭和历史恢复回归；`pnpm build`、Rust 格式/检查、198 个 Rust 测试、完整双视口 E2E（55 通过、1 跳过）和 `git diff --check` 均通过，`pnpm tauri dev` 已启动并确认 `k-Coder` 桌面主窗口加载。 |
| 2026-08-05 | 完成用户优先插入的 `P10-045` 文件修改输出与停止交互修复：根据实际会话事件确认 Grok 在完成多次 `search_repository`/`read_file` 后将 unified diff 作为普通正文输出，而非读取权限泄露；Craft 模式新增必须通过 `apply_patch`/`write_file` 落盘且不得用长代码或 diff 代替工具的契约。停止按钮与取消命令统一使用 `threadId -> turnId` 活动表，取消请求进入可见"正在停止"状态，IPC 超时后清理去重状态并允许重试。新增指令契约及桌面/窄屏正常取消、终态等待和超时重试回归；`pnpm build`、Rust 格式/检查、197 个 Rust 测试和完整双视口 E2E（55 通过、1 跳过）均通过，`pnpm tauri dev` 已启动并确认 `k-Coder` 桌面主窗口加载。 |
| 2026-08-05 | 完成用户优先插入的 `P10-044` 统一过程步骤折叠：模型上下文、逐次模型用量、自动批准、审批结果、压缩、文件变更和其他有详情的生命周期事件统一使用可展开摘要；完成的思考摘要不再因 Turn 仍活动而强制展开，进行中的思考、Pending/Running/Failed/Cancelled 工具和活动计划继续保留实时状态。阶段性说明和最终回复不进入步骤折叠区。新增模型上下文、用量、自动批准、变更、完成思考及桌面/窄屏默认折叠回归与截图；`pnpm build`、Rust 格式/检查、197 个 Rust 测试和完整双视口 E2E（53 通过、1 跳过）通过。 |
| 2026-08-05 | 完成用户优先插入的 `P10-043` 连续工具活动折叠：前端按时间线相邻关系聚合工具项，纯 `run_command` 与混合工具使用不同摘要文案；完成组默认收起，Pending/Running/Failed/Cancelled 组保持展开，思考、说明、审批和变更事件仍是分组边界。工具明细、命令输出、补丁内容、状态和耗时只改变可见性，不改写 JSONL、Provider 历史或 Compaction。更新桌面与窄屏 E2E，覆盖连续分组、单命令折叠、活动状态、失败详情和恢复顺序；`pnpm build`、Rust 格式/检查、197 个 Rust 测试和完整双视口 E2E（53 通过、1 跳过）通过。Rust 全量测试首次有一个 350ms 并发计时断言抖动，单测复跑及全量复跑均通过。 |
| 2026-08-04 | 完成用户优先插入的 `P10-042` 对话异常语义加固：新增版本 4 的 `turn_mode_selected`、`tool_started` 领域事件，重试恢复 Plan/Ask/Craft 指令和工具边界并复用应用级用户输入管理器；运行时对未处理错误执行 best-effort `finish_failed`，OpenAI Responses、Chat Completions、Gemini 与 Anthropic 只接受正常停止原因；失败 Provider 尝试用量纳入持久化、累计预算与 UI 更新，产生可见或持久化输出后禁止整请求重试；工具恢复区分 Pending/Running，跨会话队列只投影到所属线程。新增异常终态、模式恢复、截断、重试用量、输出隔离、串行工具恢复及跨线程队列回归。`pnpm build`、Rust 格式/检查、197 个 Rust 测试和完整双视口 E2E（53 通过、1 跳过）通过；标准 `pnpm tauri dev` 进程链中的桌面实例响应正常，二进制晚于本次 Rust 源码，开发服务器返回最新队列隔离与 Pending 恢复模块。未使用真实外部 Provider 凭据执行人工网络调用。 |
| 2026-08-04 | 完成用户优先插入的 `P10-041` 跨会话并行 Turn 调度：前端以 `threadId -> turnId` 活动表替代全局单活动语义，队列执行锁、取消去重、立即发送排序和队列清理均按线程隔离；当前界面只展示所选线程的队列，后台线程终态独立释放自身队列。后端继续执行每线程单 Turn 互斥并新增不同线程同时活动的契约断言；新增桌面与窄屏回归，验证第一个 `run_turn` 未结束时第二个会话可立即发起独立调用，同时保留同会话排队与显式打断行为。`pnpm build`、Rust 格式/检查、189 个 Rust 测试和完整双视口 E2E（51 通过、1 个窄屏用例按设计跳过）通过；运行中的 Tauri dev 窗口保持响应，开发服务器已加载包含按线程调度逻辑的最新模块。 |
| 2026-08-04 | 完成用户优先插入的 `P10-040` 项目会话与普通会话分离：`threadProjectMap` 中存在绑定的会话只出现在"项目"tab 的项目分组（会话数、置顶、展开和删除行为不变），"会话"tab 普通列表与搜索结果只展示未绑定项目的独立会话；映射读取提升为模块级函数并在 `useState` 惰性初始化，消除首帧因映射未加载导致的项目会话闪现。新增桌面回归断言普通列表空态与项目分组展示，narrow 视口因侧边栏隐藏按设计跳过；`pnpm build` 与 49 个前端 E2E 测试通过。 |
| 2026-08-04 | 完成用户优先插入的 `P10-039` 重试单回复归并：后端保持每次尝试使用独立 Turn，前端在 `turn_started` 实时建立 Turn 到原用户消息的映射，并按该映射把失败/取消与后续重试时间线渲染到同一个助手回复容器；JSONL 恢复后保持相同结构。新增投影契约测试及 desktop/narrow 实时与刷新恢复 E2E；`pnpm build`、Rust 格式/检查、189 个 Rust 测试和 48 个前端 E2E 测试通过，运行中的 Tauri dev 已热重编译并加载包含归并逻辑的前端资源。 |
| 2026-08-04 | 完成用户优先插入的 `P10-038` 仅保留 CodeBuddy 外观：删除 Paper、Midnight、VSCode、Amber 四套皮肤的全部 CSS 令牌块、`Skin` 类型与 `kcoder_skin` 存储，移除设置页"皮肤主题"选择器及 `.skin-card` 动画样式；`data-skin` 固定为 `codebuddy`，深/浅模式切换与 Monaco 编辑器配色不受影响。TypeScript 检查与生产构建通过。 |
| 2026-08-04 | 完成用户优先插入的 `P10-037` 合法工具复查修复：从真实 JSONL 确认同一个 `git status --short` 分别用于任务开始、修改后和最终复查，期间已有构建、测试、搜索等不同进展，旧逻辑却按整个 Turn 累计并误杀第三次调用。重复保护现改为连续调用计数：`A, A, A` 仍在第三次关闭失败，`A, B, A` 正常执行，交替无进展循环继续由既有窗口检测终止。新增拒绝与放行双分支回归；`pnpm build`、Rust 格式/检查和 188 个 Rust 测试通过，Tauri dev 已热重编译并启动可响应的新桌面进程。 |
| 2026-08-04 | 完成用户优先插入的 `P10-036` CodeBuddy 浅色外观：补充冷调蓝灰浅色令牌和 `color-scheme: light`，CodeBuddy 不再固定为深色；保留现有深色配色，外观按钮、页面恢复和 Monaco 编辑器继续按 `data-theme` 正确切换。新增 CodeBuddy 深浅模式 E2E 回归。 |
| 2026-08-04 | 完成用户优先插入的 `P10-035` Skill 文本兼容与路径诊断：确认 `impeccable/SKILL.md` 混用 LF/CRLF，`review-test-report/SKILL.md` 带 UTF-8 BOM，两者 frontmatter 均合法；现按独立 `---` 行识别闭合分隔符并仅剥离文件开头的标准 BOM，普通内容前缀和真正未闭合的 Skill 仍关闭失败。扩展诊断、模型 Skill 来源和错误文案统一隐藏 Windows 内部 `\\?\` 前缀并正确还原 UNC 路径。新增兼容、拒绝和两类 Windows 路径回归；`pnpm build`、Rust 格式/检查和 187 个 Rust 测试通过，全新 Tauri dev 实例已实际打开 Skills 设置并成功加载完整项目 Skill 注册表。 |
| 2026-08-04 | 修复项目 Skills 的有界前置元数据：压缩超长描述、补齐标准 Skill 缺少的 `triggers/risk/enabled` 字段并移除未支持的 `license/metadata` 字段；新增超长描述失败回归测试，扩展刷新后可正常发现全部项目 Skill。 |
| 2026-08-04 | 开始 `P10-001` 工作区边界威胁建模：system prompt 明确绝对工作区路径仅用于识别，`list_directory`、`read_file`、`run_command.cwd` 和 `write_file.path` 的工具 Schema 明确要求工作区相对路径，根目录使用 `.`，并新增提示词与 Schema 回归测试；保留后端对绝对路径、父目录遍历和符号链接逃逸的拒绝。 |
| 2026-08-03 | 完成用户优先插入的 `P10-034` 命令完成收口：从真实 JSONL 与恢复会话确认 `corepack pnpm build` 进程已退出但没有产生 `tool_result`，导致后续 Git 和搜索无法启动；`CommandRuntime::wait` 现以通知加 100ms 状态复核保证终态可见，Corepack 包装的包管理器构建/测试命令复用低风险分类。新增风险契约和遗漏完成通知回归；`corepack pnpm build`、`git diff --check`、Rust 格式/检查和 179 个 Rust 测试通过，Tauri dev 已热重编译并启动可响应的新桌面进程。 |
| 2026-08-03 | 完成用户优先插入的 `P10-033` 工具串行调度：移除同批工具的 `FuturesUnordered` 并发启动，严格按 Provider 返回顺序执行，前一项结果落盘和发布后才启动下一项；取消或致命失败时剩余调用不启动但仍写入配对的跳过结果。新增开始/完成顺序及取消配对回归；`pnpm build`、Rust 格式/检查和 178 个 Rust 测试通过，运行中的 Tauri dev 已自动重编译并启动新桌面进程。 |
| 2026-08-03 | 完成用户优先插入的 `P10-032` 消息队列展示调整：以 pending 队列项数量控制队列容器，空队列及仅含已完成/失败记录时不再显示"队列 (0)"；用户重复发送形成待处理项后恢复显示。新增初始隐藏和排队后显示的 E2E 断言。 |
| 2026-08-03 | 完成用户优先插入的 `P10-031` 活动 Turn 空状态展示调整：移除尚无工具或时间线条目时的"等待工具调用…"占位行及其点状动画，仅保留动态状态标题；工具执行、时间线事件、持久化和恢复逻辑不变。 |
| 2026-08-03 | 完成用户优先插入的 `P10-030` Turn 终态工具收口：修复 Tauri 热重启或会话恢复后，底部已显示"Turn 已取消"但缺少 `tool_result` 的命令仍显示"执行中"并持续计时的问题；Rust JSONL 投影新增 cancelled 工具状态，前端对实时终态和旧快照统一归一化，完成/失败终态也不再遗留 running 工具。新增真实残缺历史恢复回归和双视口取消交互断言，前端构建、Rust 格式/检查、178 个 Rust 测试及 44 个前端 E2E 测试通过。 |
| 2026-08-03 | 完成用户优先插入的 `P10-029` 命令执行状态修复：并行工具从提交顺序等待改为按实际完成顺序回传，短命令完成状态不再被前序慢命令阻塞；命令输出读取在进程终止后最多等待 500ms 排空，遗留管道句柄不再无限拖住会话终态。新增乱序完成和输出读取阻塞回归，前端构建、Rust 格式/检查及 177 个 Rust 测试通过，运行中的 Tauri dev 已自动重编译并重启桌面进程。 |
| 2026-08-03 | 完成用户优先插入的 `P10-028` 读取结果展示调整：对话时间线不再为 `read_file` 渲染"查看读取内容"折叠块或只读 Monaco，只保留路径、行号范围和状态摘要；Provider 历史、持久化工具结果及补丁/写入详情保持不变。同步更新前端 E2E 断言并清理读取预览样式。 |
| 2026-08-03 | 完成用户优先插入的 `P10-027` 工具活动摘要直显：对话时间线标题从两段式（工具名 + 目标）改为单行"动词 + 文件名 + 行号"，`read_file` 用 `L` 前缀行范围（`读取 src/App.tsx L3370-3419`），`search_repository` 带"搜索"前缀，命令/补丁/写入/浏览目录/网页同理；失败态保持工具名标题并在副标题显示后端错误，无目标工具继续回退到原工具名。状态面板最近活动同步同格式。更新桌面/窄屏 E2E 断言，`pnpm build` 与 44 个 E2E 测试通过。 |
| 2026-08-03 | 完成用户优先插入的 `P10-026` 活动状态顺序调整："思考中 / 处理工具结果中 / 整理结果中"等动态状态行移到活动 Turn 时间线底部，持续跟随最新记录，并移除该状态行的上下箭头；终态折叠摘要、事件顺序和持久化数据不变。新增实时工具记录、状态行相对位置和箭头缺失的 E2E 回归。 |
| 2026-08-03 | 完成用户优先插入的 `P10-024` 连续思考摘要分组：相邻 reasoning 事件在渲染时合并为一个"思考内容"折叠块，统一图标、标题、状态和外边界，内部以紧凑间距保留各段摘要；工具、说明和生命周期事件仍切断分组，持久化事实与 Provider 上下文不变。新增双段实时摘要的数量、内容、状态和截图回归。 |
| 2026-08-03 | 完成用户优先插入的 `P10-025` 时间线顺序修复：实时审批请求/解决事件按 `toolCallId` 锚定到对应工具之前，历史线程加载按审批快照规范化同样顺序；命令仍执行时，已完成的审批不会再显示在命令之后。相邻思考摘要只合并相同完成状态的段落。新增实时与刷新恢复的审批排序回归。 |
| 2026-08-03 | 完成用户优先插入的 `P10-023` 文件读取范围容错：从真实会话确认模型同时填充 `startLine/lineCount` 与 `offset/limit` 后，前两次读取因互斥校验失败，第三次才触发 `repeated_tool_call`；后端现在线范围优先并忽略冗余字节范围，Schema 明确优先级，旧字节范围与重复循环保护继续保留。新增复现真实参数形态的工具契约和 AgentRuntime 回归；`pnpm build`、`cargo check`、175 个 Rust 测试与真实 Tauri 自动重编译重启通过，完整格式检查仍被现有 `src-tauri/src/logging.rs` 格式差异阻塞。 |
| 2026-08-03 | 完成用户优先插入的 `P10-022` 残缺工具历史恢复：每次 Provider 请求前按 `tool_call_id` 校验调用组，只有 ID 唯一、数量一致且集合完全匹配的调用与结果会进入请求；中断或崩溃遗留的缺失、重复、错配和孤立结果被安全剔除，非空进度说明降级为普通助手文本。新增精确配对、Compaction 及真实运行时恢复回归，修复重试时 Provider 返回 HTTP 400；`pnpm build`、Rust 格式/检查、175 个 Rust 测试、44 个 E2E 测试和真实 Tauri 窗口启动均通过。 |
| 2026-08-03 | 收紧 `P10-012` 思考初始态占位：尚未收到工具活动时，"等待工具调用…"改为无边框、小字号、最多 28px 高的单行辅助状态，不再渲染占满对话宽度的大块虚线列表；新增 desktop/narrow 高度、边框和截图回归。 |
| 2026-08-03 | 完成用户优先插入的 `P10-021` 可打断消息队列：Thinking、工具执行和等待审批期间从输入区发送的内容只进入自动展开的线程绑定队列，不提前投影为用户气泡，也不自动取消当前 Turn；待处理项提供立即发送和单项删除，立即发送先把目标项提升为下一项，再复用 `cancel_turn` 请求打断，按 Turn 去重取消请求，并等待持久化终态后才投影用户消息和启动新 Turn。新增 desktop/narrow 双视口交互与截图回归；`pnpm build`、Rust 格式/检查、171 个 Rust 测试、44 个 E2E 测试和真实 Tauri 窗口启动均通过。 |
| 2026-08-03 | 完成用户优先插入的 `P10-020` 无固定调用上限与可选 Token 预算：移除单 Turn 24 次 Provider/工具调用硬停，持续取得新结果的 25 次工具 Turn 可正常完成；Goal 与子智能体默认不注入 Token 预算，显式预算继续硬性执行且累计消耗始终持久化。无进展检测改为只认可新的成功工具结果或文件内容变更，连续 3 个五轮窗口无新结果时终止；前端在无预算时显示"已用 / 无上限 tokens"，显式预算时保留百分比。新增 ADR 0013、后端安全回归和双视口交互测试。 |
| 2026-08-03 | 修复对话框图片附件的本地 OCR 发送链路：发送前可靠等待对应识别任务，避免异步状态竞争丢失 OCR 上下文；`run_turn` 对缺失识别结果执行本地 OCR 兜底，识别失败仍保留原图发送。 |
| 2026-08-03 | 完成用户优先插入的 `P10-019` 文件读取范围展示：`read_file` 运行中根据 `startLine/lineCount` 显示请求区间，完成后优先使用结果元数据中的实际 `startLine/endLine`，单行与多行分别显示为"第 N 行"和"第 N-M 行"；失败状态继续优先显示后端错误。更新历史恢复与活动 Turn 双视口断言；前端构建、Rust 格式/检查、167 个 Rust 测试及 44 个 E2E 测试通过。 |
| 2026-08-03 | 完成用户优先插入的 `P10-018` 仓库搜索授权修复：扩展刷新不再覆盖高级工具已有风险元数据，`search_repository` 继续以只读工具执行；扩展风险表中的多余键不能覆盖内置工具策略。对话时间线的失败工具优先显示后端错误而不是查询参数。新增真实扩展刷新链路、组合注册安全回归测试和 desktop/narrow 双视口失败展示测试；前端构建、Rust 格式/检查、167 个 Rust 测试及 44 个 E2E 测试通过。 |
| 2026-08-03 | 完成用户优先插入的 `P10-017` 工作台宽度调整并按反馈继续收窄：常规桌面右栏由 500–680px、42vw 缩为 360–440px、28vw，中等窗口由 420–560px、48vw 缩为 320–400px、34vw；窄屏独立面板保持不变。同步收紧三列和中等宽度回归断言，代码编辑器在新宽度下仍保持可编辑、可保存和可最大化。 |
| 2026-08-03 | 完成用户优先插入的 `P10-016` 工作台展开布局修复：对话列、消息列表、用户消息和输入区补齐 `min-width/max-width` 边界，禁止内容横向溢出到文件/Git 列；输入区底部控件允许换行，模型选择器弹性收缩，发送/停止操作固定保留在输入区右侧。新增 1536px 三列布局回归测试与截图，逐项断言消息、输入框、发送按钮和工作台边界不重叠；既有窄屏面板流程继续通过。 |
| 2026-08-03 | 完成用户优先插入的 `P10-015` 有界文件读取与代码审计：`read_file` 默认最多返回 64 KiB，支持互斥的 `startLine/lineCount` 与兼容的 `offset/limit`，显式范围仍受 256 KiB 上限约束，并返回行号、字节数、总行数和前后省略状态。对话时间线不再把读取结果二次裁为 64 KiB，而是懒加载只读 Monaco，逐字展示本次实际进入 Provider 历史的工具结果，并按文件类型高亮、按请求范围显示真实行号。新增 Rust 契约测试及桌面/窄屏交互与截图验证。 |
| 2026-08-03 | 完成用户优先插入的 `P10-014` 思考内容展示：活动 Turn 中安全摘要完成后仍持续展开，进入终态后才随执行过程默认折叠；Gemini `generateContent` 推理请求启用官方 `includeThoughts`，将 `thought: true` 的流式摘要接入既有有界事件、持久化和时间线链路，普通回答保持独立，非标准或原始推理内容继续忽略。新增 Gemini 摘要解析和活动期展开回归测试。 |
| 2026-08-03 | 在工作台资源管理器顶部接入 `WorkspacePicker`（之前已实现但未挂载），新增 `compact` 模式：缩为 30px 高、隐藏副标题路径、缩小图标和圆角，与 `panel-toolbar` (38px) 协调；不再抢占"刷新"等工具按钮的可见空间。侧边栏等其他位置继续使用原高度样式。同时将"切换工作台"独立入口放到顶部 titlebar：`PanelsLeftRight` 图标按钮（runtime-state 之后、主题按钮之前），与 conversation-header 的 `PanelRight` 按钮共享职责——控制工作台面板的开/关（`setWorkbenchOpen`），并通过 `icon-button--active` 反映当前状态。工作台第三列宽度恢复为 `232px minmax(0, 1fr) 360px`（之前 `clamp(500px, 42vw, 680px)` 在大屏下挤压对话区）。conversation-header 中移除"切换工作台面板"按钮和"归档会话"按钮（重复且冗余，titlebar 的 `PanelsLeftRight` 已可控制工作台开关，归档功能暂未启用），仅保留"子智能体"按钮；同时清理未使用的 `PanelRight` / `Archive` import 和 `archiveActiveThread` 解构。 |
| 2026-08-03 | 完成用户优先插入的 `P10-013` 工作台文件编辑：文件树缩为 12px/23px 紧凑行并显示选中项；文本文件打开后工作台扩为 500–680px，文件树缩至 20%，并支持一键最大化代码区。本地打包 Monaco Editor，支持行号、语法高亮、当前行、Tab 编辑及 `Ctrl/Cmd+S`。新增 `save_workspace_file` 类型化命令，仅允许 256 KiB 内 UTF-8 文本，复用工作区规范化边界与 Patch 进程内编辑锁，并用预览 SHA-256 拒绝陈旧保存；新增成功、冲突、越界、二进制及桌面/窄屏编辑保存和尺寸测试。 |
| 2026-08-03 | 修正工作台响应式布局：中等宽度改为对话区与工作台独立双列，避免面板覆盖对话；仅在窄屏保留右侧浮层。修复通用按钮选择器误作用于工作区选择器导致名称溢出的问题，并增加中等宽度布局回归测试。 |
| 2026-08-03 | 修正 `run_command` 取消收尾：外部命令收到 Turn 取消后最多等待 2 秒的进程树收尾窗口，避免 stdout/stderr 子进程未关闭时阻塞整个 Turn；补充慢命令取消回归测试。 |
| 2026-08-03 | 修正侧边栏会话/项目归类与项目添加：`open({ directory: true, multiple: true })` 支持一次添加多个项目，多余项目进入 `kcoder_known_projects` 并立刻在"项目"tab 显示（暂无会话时显示空态）；移除"未分类"虚拟项目，"会话"tab 下 `+` 与 `Ctrl+N` 创建的会话不再被自动归到当前工作区；"项目"tab 中项目 `+` 创建的会话即时绑定到该项目路径；项目删除同步清理 `known` 列表和折叠状态。 |
| 2026-08-03 | 优化 `P10-012` 活动 Turn 反馈：移除对话中的 k-Coder 头像和运行期外层"正在执行"折叠行，运行工具改用进行时标题、旋转图标、横向不定进度和实时耗时；工具完成到下一次 Provider 输出之间显示紧凑活动状态，避免时间线看似停滞。完成后的总耗时折叠与完整审计时间线保持不变，新增桌面/窄屏活动文件读取回归截图。 |
| 2026-08-03 | 完成用户优先插入的 `P10-012` Turn 执行过程折叠：核对 Codex 本地源码后明确区分 TUI/客户端的 `Worked for` 视觉分隔、完成通知的 Summary 载荷与 Core token 阈值 Compaction。k-Coder 终态协议新增开始、完成和总耗时，JSONL 投影可恢复终态耗时；活动 Turn 保持展开，完成/失败/取消后默认折叠过程，最终回答始终独立可见，展开后仍可审计完整时间线。新增实时、刷新恢复及桌面/窄屏 E2E。 |
| 2026-08-03 | 修正 `P2-010/P5-004/P5-007/P5-008` Token 与 Compaction 语义：普通 Turn 不再因多次 Provider 请求累计达到固定 1,000,000 tokens 而误停，累计用量仅用于统计；Goal/子智能体显式预算继续硬性执行且最大为 1,000,000。自动压缩同时参考本地估算和最近一次 Provider 当前窗口用量，达到模型窗口 80% 后在 Turn 中压缩继续；压缩恢复把近期工具结果转换为有界文本，避免孤立工具输出。新增累计 1,160,800 tokens 正常完成、显式预算失败、Provider 用量触发压缩和恢复协议完整性回归测试。 |
| 2026-08-03 | 补强 `P2-009/P3-009/P4-003` 文件工具可观测性：读取文件显示路径、范围并可展开有界内容；`apply_patch` 即使语法校验失败也从补丁头显示目标文件并可展开原始补丁，成功变更继续展示审计后的逐文件 Diff。上下文压缩仍以独立时间线事件明确展示。OCR `context` 块及旧式识别标记不再污染自动会话标题。 |
| 2026-08-03 | 调整 `P6-006` 图片与 OCR 展示：输入区不再显示识别行数、识别正文、失败状态或悬停结果；支持不输入文字直接发送图片，发送后及历史恢复时，用户消息以紧凑图片标签保留附件名称。OCR 和图片分析提示作为有界 `context` 内容块后台传给模型，不混入可见用户正文。新增双视口 E2E，覆盖发送前、发送后与刷新恢复。 |
| 2026-08-02 | 修复历史线程加载的 Tauri 参数契约：`get_plan`、`get_goal`、`retry_turn` 和 `cancel_turn` 统一发送 `threadId`，避免旧的 `thread_id` 导致历史记录打开时报 `missing required key threadId`。新增 E2E 断言覆盖计划和目标恢复参数。 |
| 2026-08-02 | 扩展 `P6-006` 图片附件能力：接入随应用打包的 PP-OCRv5 mobile 检测/识别模型和 Windows ONNX Runtime，图片添加后异步本地识别并显示可展开文字；发送时保留原图，同时把识别文本作为有界上下文提供给模型，OCR 失败不阻塞原图发送。新增 Tauri 资源映射、输入大小校验和 OCR 解析测试。 |
| 2026-08-02 | 补强 `P9-001/P6-010` 主动澄清行为：Craft、Ask 和 Plan 模式在无法从工作区确定用户意图且错误假设会改变结果时，都可通过 `request_user_input` 展示结构化多选问题；用户回答后继续同一 Turn，并新增 Craft 模式提示契约测试。 |
| 2026-08-02 | 补强 `P2-009/P3-009/P4-003/P6-009` 工具可观测性：命令活动可展开查看 `program`、参数、工作目录和超时，实时与历史记录显示耗时；文件变更在对话时间线提供逐文件 unified diff 入口。`run_command` 持久化开始/结束/耗时元数据，旧事件保持兼容；新增双视口命令、耗时和变更详情 E2E。 |
| 2026-08-02 | 补强 `P1-011/P6-010` 对话交互：Thinking、工具执行和等待审批期间输入框保持可编辑，发送消息进入绑定线程的 FIFO 队列；队列处理增加并发锁，停止/失败/取消终态后自动继续下一项，停止按钮与发送按钮并列保留。新增双视口队列、打断和继续执行 E2E，当前前端测试为 34/34。 |
| 2026-08-02 | 完成用户优先插入的 `P10-011` 会话级审批模式：输入区可切换"请求批准 / 完整访问"，默认请求批准且重启不保留完整访问；主智能体和子智能体共享后端模式快照，活动任务期间禁止切换。完整访问自动批准已注册工具，但继续执行 Schema、工作区路径、Patch 哈希、Hooks、命令超时/取消和进程树清理，并为每次操作持久化审批请求与解决审计。新增后端自动批准/审计、运行中切换拒绝和双视口界面测试。 |
| 2026-08-02 | 修正 `P2-010` 运行时上限误杀复杂任务：Provider 迭代上限从 8 调整为 24，与工具调用上限对齐；新增默认单 Turn 1,000,000 Token 硬预算，继续保留响应、上下文、工具输出和重复调用边界。新增超过 8 轮仍可完成的回归测试。 |
| 2026-08-02 | 修复完整 Turn 时间线与恢复一致性：Provider 上下文、逐次用量、压缩、审批、用户输入、任务清单、变更和完成/失败/取消终态均投影为有界独立事件；用户输入、任务清单、用量和终态遵循先落盘后发布，线程加载期间实时事件进入 hydration buffer；失败/取消/重试按原用户消息锚定，命令最终结果保留有界 stdout/stderr chunks。新增 schema 3 迁移、用户输入落盘顺序、终态/孤儿排序、刷新恢复和 hydration 竞态测试；当前 Rust 144/144，前端双视口 30/30。 |
| 2026-08-02 | 完成 `P1-008/P2-009/P3-006/P4-003/P5-009/P6-009` 对话与自修改闭环验收：前端以 Turn 时间线作为文字、推理摘要和工具活动的唯一实时事实源，按调用与输出游标去重；取消保持忙碌直到持久化终态，重试不重复用户消息，队列绑定原线程；普通工具失败会回送模型继续修复，审计/存储失败则回滚并以类型化错误终止。新增"编辑失败 -> 测试失败 -> 修复 -> 测试通过 -> 刷新恢复"和取消重试双视口 E2E；完整结果为前端 26/26、Rust 141/141。 |
| 2026-08-02 | 补强 `P1-008/P2-009` 助手输出展示：最终回复、流式说明和安全推理摘要统一使用禁用原始 HTML 的 GFM Markdown 渲染；标题、列表、表格、引用、行内代码、代码块和任务列表获得响应式样式，代码块支持复制，远程 Markdown 图片不自动加载。 |
| 2026-08-02 | 修复 `P3-006/P5-004` 并行工具审批：前端按请求 ID 使用 FIFO 队列依次展示同一 Turn 的多个审批，过期请求只移除对应卡片；线程恢复会取消全部未决审批，避免单槽覆盖或重启后点击"运行"返回 `approval request was not found`。 |
| 2026-08-02 | 补强 `P1-008/P2-009/P4-003/P4-009` 实时交互：OpenAI Responses 显式请求并只投影安全 reasoning summary，原始 reasoning text 不进入公共协议；AgentRuntime 新增独立 Thinking/回复/工具/审批/收尾状态；`run_command` 将已脱敏 stdout/stderr 通过有界队列批量流入对应工具行，安全摘要可随会话恢复，高频状态和命令增量保持瞬时。 |
| 2026-08-02 | 修复 `P2-009/P6-009` 对话执行过程展示：工具调用前的可见进度说明随调用持久化并进入后续 Provider 上下文，`read_thread` 新增按事件顺序恢复的 Turn 时间线；界面实时交错展示说明、单个操作状态和最终回答，不再在完成后仅显示"执行了 N 个操作"的折叠汇总。 |
| 2026-08-02 | 调整 `P6-009/P9-001` 计划展示：持久计划与工具活动按 Turn 内嵌到对话时间线，历史消息恢复 Turn 关联；工作台聚焦文件和 Git，代码变更继续从对应回复进入 Diff 审阅。 |
| 2026-07-25 | 修复 `P6-002/P6-004` 工作台回归：文件树按常见格式显示 VS Code 风格的彩色图标；Git 面板可正确暂存未跟踪文件和全部更改，显示操作反馈，首次推送自动建立 upstream，并补充提交、拉取、推送及 Unicode 路径回归测试。 |
| 2026-07-25 | 扩展 `P6-007` 多供应商运行时：单配置升级为带活动项的供应商集合，旧 `provider.json` 和前端本地列表自动迁移；API Key 按供应商 ID 使用独立系统凭据槽，输入区可切换已就绪供应商及其模型，未配置密钥的供应商禁止激活。 |
| 2026-07-25 | 修复 `P6-007` 输入区模型选择器在显示名称与模型 ID 仅大小写不同时重复展示模型名称的问题；两者语义相同时只显示名称，确有差异时继续显示 ID，并补充双分支 E2E 回归断言。 |
| 2026-07-25 | 扩展 `P6-007` 模型管理：模型列表升级为可新增、编辑、删除和选择默认项的结构化列表，每项保存模型 ID、显示名称和上下文长度；活动模型上下文长度接入主智能体、手动压缩和子智能体运行时，旧字符串列表继续自动迁移。 |
| 2026-07-25 | 修正 `P6-007` 模型配置语义：供应商配置新增名称、模型列表和默认模型，旧单模型配置兼容迁移；输入区选择器改为按真实供应商展示其模型，移除硬编码跨供应商目录，并补齐窄屏设置入口和双视口 E2E。 |
| 2026-07-24 | 完成 Phase 8 验收并交付 `0.9.0-multi-agent`：子智能体复用 `AgentRuntime`，创建/等待/消息/恢复/列表/关闭契约、深度/并发/Token/时长限制、工作区与 Capability 收缩、父子取消、写冲突保护、JSONL 生命周期与有界摘要、同窗活动界面和确定性并行评估已接通，当前位置推进到 `P9-001`。 |
| 2026-07-25 | 完成 Phase 9 验收并交付 `0.10.0-advanced-agent`：持久计划、宿主强制 Goal Token/时间预算、独立审批的浏览器会话与截图制品、确定性仓库搜索、可溯源可删除记忆、请求前故障切换、多端点、有界 PDF/DOCX/文本提取、录制夹具评估和运行指标已接通，当前位置推进到 `P10-001`。 |
| 2026-07-19 | 完成 Phase 7 验收并交付 `0.8.0-extensible-agent`：分层规则、严格 Skill 发现与风险启用、执行前 Skill 注入、MCP stdio/Streamable HTTP 生命周期、工具命名空间、系统凭据注入、统一策略与通用审批、前后 Hooks、诊断/启停/审计和双视口设置 E2E 已接通，当前位置推进到 `P8-001`。 |
| 2026-07-19 | 完成 Phase 6 验收并交付 `0.7.0-workbench`：持久工作区、懒加载文件树与预览、Git 状态/分支/确认式写操作、四 Provider 原生图片附件、有界文档提取、连接测试与用量、会话管理、持久化计划与变更审阅、无障碍响应式交互和桌面/窄屏 E2E 已接通，当前位置推进到 `P7-001`。 |
| 2026-07-19 | Phase 6 启动：开始 `P6-001` 项目注册表、最近工作区和工作区切换。 |
| 2026-07-19 | 完成 Phase 5 验收并交付 `0.6.0-resilient-agent`：JSONL 事实事件、可重建 SQLite 投影、逐次与逐 Turn 用量、预算化上下文、手动/自动 Compaction、约束与工具结果保留、孤儿 Turn/命令恢复、事件迁移和脱敏轮转日志已接通，当前位置推进到 `P6-001`。 |
| 2026-07-19 | 完成 Phase 4 验收并交付 `0.5.0-execution-agent`：结构化前台/后台命令会话、有界游标输出、跨平台超时与进程树清理、状态/等待/输入/关闭、运行时风险审批、宿主可复用授权、ConPTY/Unix PTY、敏感信息脱敏和集成测试已接通，当前位置推进到 `P5-001`。 |
| 2026-07-19 | 启动 Phase 4：实现结构化命令会话、有界游标输出、超时/取消、进程树终止、状态/等待/输入/关闭、风险分类、宿主可复用授权、输出脱敏、智能体工具与类型化 IPC；PTY 后端及 Rust 集成验证尚未完成。 |
| 2026-07-19 | 完成 Phase 3 验收并交付 `0.4.0-edit-agent`：严格 Patch、新增/修改/删除/移动、完整文件替换、审批生命周期、哈希冲突保护、事务回滚、Diff 审阅、变更快照与撤销已接通，当前位置推进到 `P4-001`。 |
| 2026-07-18 | 完成 Phase 2 验收并交付 `0.3.0-read-agent`：四种 Provider 原生工具调用、只读工作区工具、结构化历史、工具活动记录和循环保护已接通，当前位置推进到 `P3-001`。 |
| 2026-07-18 | 模型供应商新增 OpenAI Responses、Anthropic Messages 和 Google Gemini 流式协议，旧配置继续默认使用 OpenAI Chat Completions。 |
| 2026-07-18 | 按用户优先级提前建立可扩展设置中心，模型供应商配置已接入，MCP、插件、Skills、机器人、Workflows、知识库、规则和通用设置保留独立入口；`P6-007` 标记为进行中。 |
| 2026-07-18 | 完成 Phase 1 验收并交付 `0.2.0-chat`，当前位置推进到 `P2-001`。 |
| 2026-07-18 | 将项目文档和界面文案统一为中文，保留必要技术标识。 |
| 2026-07-18 | 完成 Phase 0 验证，将当前位置推进到 `P1-001`。 |
| 2026-08-02 | 新增全局推理强度配置：输入区提供关闭/最低/低/中/高/最高六档选择，配置持久化并由 OpenAI Chat/Responses、Anthropic、Gemini 适配器分别映射兼容字段；新增 Rust 持久化与契约测试、双视口前端选择器回归。 |
| 2026-07-18 | 创建 Phase 0-11、验收门槛、持续质量工作和 cn-codex 成熟度复核。 |
