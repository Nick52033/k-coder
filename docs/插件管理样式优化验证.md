# 插件管理样式优化验证

日期：2026-09-14。用户优先插入任务：P10-180。

## 设计与范围

使用仓库内 `src/resources/skills/ui-ux-pro-max/SKILL.md`，执行本地设计系统与 React 规则检索。采用开发工具的紧凑单列结构，颜色、字体继续复用现有语义令牌及浅/深色主题。

- 标题增加简短说明，刷新按钮增加文字；项目插件目录独立为带标签的信息条。
- 列表展示后端发现数量及启用意图数量；已启用不等于已加载或完全可用。
- 插件采用轻边框卡片，名称、版本、状态、能力标签、路径和异常提示分层展示。
- 超过 160 字符的描述默认显示两行，通过原生 `details/summary` 展开全文，支持键盘 Enter 收起/展开。
- 开关及删除按钮的点击区域为 44×44px；窄内容区域的操作移至卡片底部，长路径可换行。
- 完善初始加载、空列表指引、删除后果说明、焦点和减少动态效果样式。

源码变更仅在 `src/components/PluginSettingsPage.tsx` 和 `src/components/PluginSettingsPage.css`；未改变 Provider、插件执行、授权、删除命令或公共协议。复用已有插件契约与安全回归。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| `pnpm build` | 通过；保留既有大 chunk 提示 |
| `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` | 通过 |
| `cargo check --manifest-path src-tauri/Cargo.toml` | 通过 |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 545 通过、3 失败；名称与 P10-175/P10-177/P10-179 已记录问题一致 |
| `pnpm exec playwright test e2e/workbench.spec.ts -g "local plugin" --workers 2`（端口 1485） | 6 通过、2 按配置跳过；覆盖启停、异常恢复、删除确认/取消、空列表、深色及 375–1440px 布局 |
| 隔离原生 Tauri 页面验收 | 通过，见下文 |
| 变更文件 `git diff --check` | 通过 |

三个 Rust 失败均在读取收敛逻辑，本次未修改 Rust：

- `agent::tests::read_recovery_delivery_only_hard_stops_the_corrected_provider_batch`
- `agent::tests::recovery_still_stops_varied_overlapping_reads_after_one_correction`
- `agent::tests::semantic_read_tracker_recovers_once_before_stopping_overlap_loops`

## 原生验收

通过 `pnpm tauri dev --no-watch --config src-tauri/target/plugin-style-validation.json` 启动独立 identifier `com.kcoder.validation.pluginstyle`，Vite 端口 1486、WebView2 调试端口 9416。测试实例登记当前源码工作区并建立隔离测试会话，避免刷新回到启动目录。

真实后端在验收时返回 7 个插件、0 个启用，页面数量与后端一致。验证刷新、删除确认后取消、完整说明的键盘展开与收起、刷新按钮焦点及减少动态效果。未启用或删除真实插件，未验证插件执行能力。

浅/深色各测 375、700、900、1280px，共 8 组：页面、内容区、卡片横向溢出均为 0；点击区域至少 44×44 CSS px（忽略 WebView 浮点舍入误差）。375/700px 使用 WebView 视口模拟，原生窗口仍保留项目既有最小宽度。删除遮罩经实测覆盖 1280×820 整个视口。

验证脚本与结果保存在忽略目录 `src-tauri/target/validate-plugin-style.cjs`、`plugin-style-native-result.json`；构建和 Rust 测试日志同目录保存为 `plugin-style-build.log`、`plugin-style-rust-test.log`。

截图：

- [深色桌面](plugin-settings-native-dark.png)
- [浅色桌面](plugin-settings-native-light.png)
- [浅色窄视口](plugin-settings-native-light-narrow.png)
- [深色窄视口](plugin-settings-native-dark-narrow.png)

本次未部署到 `D:\apps\k-coder\`，未执行 git commit 或 git push。既有插件文件及其他进行中的源码改动由其原任务维护。
