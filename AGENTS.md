# AGENTS.md

本说明适用于整个 k-Coder 仓库。

## 必读文档

修改代码前必须阅读：

1. `docs/sys/开发路线图.md`
2. `docs/sys/架构.md`

机器人工作流相关改动另需阅读 `docs/sys/机器人工作流设计.md`。

## 文档约定

- `docs/` 现只保留 `docs/sys/` 下 7 份架构与设计文档：`开发路线图.md`、`架构.md`、`机器人工作流设计.md`、`扩展.md`、`知识库开发设计.md`、`知识库与记忆扩展详细设计.md`、`自修改操作说明.md`。其余文档（60 份 ADR、全部验证记录与原生取证产物、specs/plans、专项计划）已按用户要求从仓库移除。
- 除非用户明确要求，不要往 `docs/` 新增文件。有长期约束力的设计结论并入上面七份之一，过程性记录只留在对话、提交信息与验收报告里。
- 正文里遗留的 “ADR 00xx” 与 `docs/sys/xxx验证.md` 引用只作历史索引，不再指向可读文件。
- 不要在仓库根目录或其他位置散落生成 `.md` 文件。
- `AGENTS.md` 必须留在仓库根：运行时只从工作区根目录加载规则，移动即失效。

## 协作习惯

- 用户描述一个功能或改动意图（而非提问）时，视为**要求直接修改本项目**，先按正常流程实现并验证，不需要再确认"是否要我改"。
- **默认不提交代码**：完成的改动停留在工作区，不执行 `git commit` / `git push`，除非用户明确说"提交"或主动要求提交。

## 开发流程

1. 在路线图中找到“当前位置”。
2. 除非用户明确调整优先级，否则只处理其中列出的“下一项任务”。
3. 当前阶段未通过验收门槛时，不得提前开发后续阶段功能。
4. 每个公共契约和安全分支都必须新增或更新测试。
5. 执行当前阶段规定的验证命令。
6. 完成任务前同步更新路线图复选框、当前位置和变更记录。

## 架构边界

- `src/` 只包含界面和展示状态。
- `src-tauri/src/commands/` 是轻量 Tauri 边界，不得包含智能体循环逻辑。
- `agent/` 负责协调 Turn，不实现具体模型协议或工具行为。
- `providers/` 负责把外部模型协议转换为内部 Provider 事件。
- `tools/` 负责工具 Schema 和处理器，授权决策属于 `policy/`。
- `storage/` 负责持久化领域事件并提供 Repository，界面不得直接访问数据库。
- `protocol/` 存放跨边界传输的版本化载荷。
- 子智能体必须复用主智能体运行时，不得另写第二套智能体循环。

## 安全约束

- 文件访问前必须解析并规范化所有路径。
- 必须拒绝工作区之外的路径，包括符号链接和目录联接逃逸。
- 模型传入的权限字段不能作为授权依据。
- Shell 命令必须经过策略判断，并支持超时、取消和进程树清理。
- 禁止记录 API Key、授权请求头、完整环境变量或用户密钥。
- 破坏性操作必须经过明确审批，并生成可审计事件。

## 质量门槛

至少执行：

```powershell
pnpm build
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo check --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml
```

未启动 `pnpm tauri dev` 并实际验证变更路径时，不得声称桌面工作流已经完成。

## 本地运行与源码修改约定

- 客户端运行/安装目录固定为 D:\apps\k-coder\。
- 代码、测试和文档修改只在源码仓库（当前工作区根目录 `D:\code\k-coder\`）中进行；不得把源码改动写入安装目录。
- 需要验证安装端行为时，先构建源码，再明确记录是否部署到安装目录。
