# AGENTS.md

本说明适用于整个 k-Coder 仓库。

## 必读文档

普通对话和代码分析默认参考 `docs/sys/架构.md`：先按标题定位并读取涉及的架构边界、安全约束、协议或数据流章节；只有跨越多个边界或修改总体架构时才通读全文。**普通对话不要读取 `docs/sys/开发路线图.md`。**

只有实际执行代码修改时，才额外读取路线图并按任务范围读取，不把整份历史文档重复注入每个对话：

1. `docs/sys/开发路线图.md`：读取“当前位置”“当前插入任务”和与本任务直接相关的最新变更记录；对超长表格单元只读取有界摘要（建议每项最多 4 KiB），只有需要追溯历史回归或改变阶段边界时才读取更早记录。

机器人工作流相关改动另需读取 `docs/sys/机器人工作流设计.md` 的对应节点和运行时约束章节。

读取结果应在对话中保留简短上下文摘要，后续回合复用摘要和已读章节，不重复加载整份路线图或历史验收记录。

## 文档约定

- `docs/` 现只保留 `docs/sys/` 下 7 份架构与设计文档：`开发路线图.md`、`架构.md`、`机器人工作流设计.md`、`扩展.md`、`知识库开发设计.md`、`知识库与记忆扩展详细设计.md`、`自修改操作说明.md`。其余文档（60 份 ADR、全部验证记录与原生取证产物、specs/plans、专项计划）已按用户要求从仓库移除。
- 除非用户明确要求，不要往 `docs/` 新增文件。有长期约束力的设计结论并入上面七份之一，过程性记录只留在对话、提交信息与验收报告里。
- 正文里遗留的 “ADR 00xx” 与 `docs/sys/xxx验证.md` 引用只作历史索引，不再指向可读文件。
- 不要在仓库根目录或其他位置散落生成 `.md` 文件。
- `AGENTS.md` 必须留在仓库根：运行时只从工作区根目录加载规则，移动即失效。

## 协作习惯

- 用户描述一个功能或改动意图（而非提问）时，视为**要求直接修改本项目**，先按正常流程实现并验证，不需要再确认"是否要我改"。
- **默认不提交代码**：完成的改动停留在工作区，不执行 `git commit` / `git push`，除非用户明确说"提交"或主动要求提交。

## 开发流程

1. 在路线图中找到“当前位置”。
2. 只处理用户明确规划或要求执行的任务；路线图中未列出的任务不由助手自行认定。
3. 当前阶段未通过验收门槛时，不得提前开发后续阶段功能。
4. 每个公共契约和安全分支都必须新增或更新测试；测试的**编写**是随改动交付的，测试的**执行**按第 5 条分层触发，两者不是同一件事。
5. 按改动风险分层执行验证命令。日常 Turn 的通过标准是「编译/构建不报错」，默认不跑未受影响的测试；完整套件只在阶段收口、发布前或用户明确要求时执行，跨边界改动只提高日常验证档位、不是全量触发器。
6. 完成任务前同步更新路线图复选框、当前位置和变更记录。

## 架构边界

- `src/` 只包含界面和展示状态。
- `src-tauri/src/commands/` 是轻量 Tauri 边界，不得包含智能体循环逻辑。
- `agent/` 负责协调 Turn，不实现具体模型协议或工具行为。
- `providers/` 负责把外部模型协议转换为内部 Provider 事件。
- `tools/` 负责工具 Schema 和处理器，授权决策属于 `policy/`。
- `storage/` 负责持久化领域事件并提供 Repository，界面不得直接访问数据库。
- `protocol/` 存放跨边界传输的版本化载荷。
- 子智能体必须复用主智能体运行时，不得另写第二套智能体循环。

## 安全约束

- 文件访问前必须解析并规范化所有路径。
- 必须拒绝工作区之外的路径，包括符号链接和目录联接逃逸。
- 模型传入的权限字段不能作为授权依据。
- Shell 命令必须经过策略判断，并支持超时、取消和进程树清理。
- 禁止记录 API Key、授权请求头、完整环境变量或用户密钥。
- 破坏性操作必须经过明确审批，并生成可审计事件。

## 质量门槛

验证范围必须覆盖所有受影响的公共契约和安全分支，但不因小改动自动扩大到全仓。按下面的最小集合执行：

「完整门槛」是收口动作，不是每轮动作：只在阶段收口、发布前或用户明确要求时执行。跨边界改动只把日常验证提高到下表第四行，不触发全量。

| 改动范围 | 日常验证（默认通过标准） | 完整门槛 |
| --- | --- | --- |
| 仅文档、路线图或注释 | 文档结构、链接和 `git diff --check` | 不需要 |
| 仅 `src/`、CSS 或展示状态 | `pnpm build` 即通过；只在改动落在已有 Playwright 用例覆盖的行为上时跑该用例 | 阶段收口、发布前或用户明确要求时跑前端全量回归 |
| Rust 单模块、纯函数或内部逻辑 | `cargo fmt --check` + `cargo check` 即通过；只在第「测试触发条件」命中时追加受影响模块测试 | 阶段收口、发布前或用户明确要求时跑全量 Rust |
| IPC、Provider、AgentRuntime、工具、存储、协议或安全边界 | `cargo fmt --check` + `cargo check` + 直接相关的集成目标（`tests/` 下对应文件）；模块测试同样只在触发条件命中时追加 | 阶段收口、发布前或用户明确要求时跑全量 Rust |
| 桌面启动、窗口、WebView 或安装端行为 | 相关构建和定向 native/Playwright 验收 | 发布前跑完整桌面验收 |

### 测试触发条件

日常 Turn **默认不跑 `cargo test` 与 Playwright**，只在下列任一条件成立时执行对应范围，并且只跑一次：

- 改动了某个已有测试所断言的行为 → 跑该测试所在模块；
- 新增或修改了测试代码 → 跑它本身；
- 触及安全分支（路径规范化、工作区外路径拒绝、权限字段、Shell 策略、凭据脱敏）→ 必须跑对应测试，不得省略；
- 改动将作为公共契约交付、或用户明确要求验证 → 跑受影响范围；
- 无法通过编译/类型检查判断正确性，且错误代价高 → 跑最小可判定范围，并在进度说明里写清为什么必须跑。

以上都不成立时，`cargo check` 或 `pnpm build` 通过即为该 Turn 的验证完成，不得因为「更安全」而追加未受影响的测试。

本项目后端是单个 lib crate，任何 `cargo test` 都会重编译整个 crate 并链接测试二进制；模块过滤只减少运行量，不减少编译量。因此 `cargo check` 是日常默认动作，`cargo test` 必须有上表的触发理由。

完整 Rust 门槛（仅在上述时机执行）为：

```powershell
pnpm build
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo check --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml
```

日常 Turn 禁止：

- 跑不带模块过滤的全量 `cargo test --lib`；
- 在没有命中「测试触发条件」时跑 `cargo test`；
- 为了「确保没问题」而反复重跑同一条已经通过的验证命令；
- 执行 Release 构建或部署（`pnpm tauri build`、`pnpm build:deploy`、`scripts/k-coder-build-deploy.bat`）——需要验证安装端行为时按「本地运行与源码修改约定」单独进行；
- 每改一处就验证一轮——一个 Turn 内多次编辑后只在改完时验证一次，不为中间态反复整 crate 重编译；
- 工作区树编译不过时对他人在途改动做大面积修补——先报告阻塞的具体错误，交由负责该改动的对话收尾。

`--test-threads=1` 只用于已知存在时序竞争的专项或最终稳定复跑；日常迭代先使用默认并行测试。多个对话共享同一工作区时，同一时间只允许一个对话执行完整门槛，其余对话执行定向验证；所有改动合并后由一个对话负责最终全量验证。

未启动 `pnpm tauri dev` 并实际验证变更路径时，不得声称桌面工作流已经完成。

## 本地运行与源码修改约定

- 客户端运行/安装目录固定为 D:\apps\k-coder\。
- 代码、测试和文档修改只在源码仓库（当前工作区根目录 `D:\code\k-coder\`）中进行；不得把源码改动写入安装目录。
- 需要验证安装端行为时，先构建源码，再明确记录是否部署到安装目录。
